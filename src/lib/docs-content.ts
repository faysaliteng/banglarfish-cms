// AUTO-GENERATED in-admin documentation (Help & Developer docs). Rendered by /admin/docs.
export type DocSection = { slug: string; title: string; category: string; icon: string; summary: string; html: string };

export const DOC_CATEGORIES: string[] = ["Getting Started","Overview","Catalog","Sales","Content","Appearance","Configuration","Developer"];

export const DOCS: DocSection[] = [
  {
    "slug": "getting-started",
    "title": "Getting Started",
    "category": "Getting Started",
    "icon": "Rocket",
    "summary": "A tour of the admin panel plus a first-day checklist: branding, categories, products, theme, payments, SMS, and delivery.",
    "html": "<p>Welcome! Your store is a self-hosted e-commerce CMS built on <strong>TanStack Start</strong> with a <strong>Postgres</strong> database, and everything is managed from one admin panel. This guide shows you where things live and what to do on your first day.</p>\n\n<h3>Logging in</h3>\n<p>Open <code>/auth</code> and sign in, then go to <code>/admin</code>. You need a <strong>staff</strong>, <strong>manager</strong>, or <strong>admin</strong> role. If you see a \"Not authorized\" screen, ask an existing admin to grant your account access.</p>\n\n<h3>The big picture</h3>\n<p>The left sidebar is your map. It is split into six groups:</p>\n<ul>\n<li><strong>Overview</strong> — Dashboard, Analytics, and Visitors &amp; IPs.</li>\n<li><strong>Catalog</strong> — Products, Categories, and the Media Library.</li>\n<li><strong>Sales</strong> — Orders, Coupons, Shipping, and Delivery Areas.</li>\n<li><strong>Content</strong> — Homepage, Pages (CMS), Blog, Recipes, Banners, Menus, and Reviews.</li>\n<li><strong>Appearance</strong> — Theme &amp; Layout and Branding.</li>\n<li><strong>Configuration</strong> — Payment Gateways, SMS Gateway, Social Login, SEO Engine, Users &amp; Roles, and Settings.</li>\n</ul>\n<p>Press <code>Ctrl+K</code> (or <code>⌘K</code>) anywhere to open the command palette and jump straight to any page.</p>\n\n<h3>First-day checklist</h3>\n<ol>\n<li>Open <strong>Appearance &rarr; Branding</strong> and set your store name, logo, and colors.</li>\n<li>Go to <strong>Catalog &rarr; Categories</strong> and create a few categories to organize your shop.</li>\n<li>In <strong>Catalog &rarr; Products</strong>, add your first products, assign them to categories, and upload images through the Media Library.</li>\n<li>Visit <strong>Appearance &rarr; Theme &amp; Layout</strong> to pick a theme and arrange the storefront.</li>\n<li>Under <strong>Configuration &rarr; Payment Gateways</strong>, connect how you take money, then set up <strong>SMS Gateway</strong> for order notifications.</li>\n<li>Open <strong>Sales &rarr; Delivery Areas</strong> to define where you deliver and any per-area charges.</li>\n<li>Finish by shaping <strong>Content &rarr; Homepage</strong> and <strong>Banners</strong> so the landing page feels like yours.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Do branding and categories <em>before</em> adding products — it saves re-work later.</li>\n<li>Check the <strong>Dashboard</strong> daily for new orders and sales at a glance.</li>\n<li>Place a test order yourself (add to cart &rarr; checkout) before going live.</li>\n<li>Keep at least one other trusted admin in <strong>Users &amp; Roles</strong> as a backup.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>The panel is client-rendered and gated by role. Pages use file-based routing under <code>src/routes</code>:</p>\n<pre><code>/auth            login\n/admin           Dashboard (ssr: false)\n/admin/products  Products\n/admin/delivery  Delivery Areas</code></pre>\n<p>Point the app at your Postgres instance through environment configuration before starting the server.</p>"
  },
  {
    "slug": "use-for-any-product",
    "title": "Using it for any product",
    "category": "Getting Started",
    "icon": "Boxes",
    "summary": "Rebrand the store and swap categories, products, homepage copy, logo and theme to sell fashion, electronics, groceries — anything, not just fish.",
    "html": "<p>The demo ships as a fish shop, but <strong>nothing in the engine is fish-specific</strong>. Every product name, category, price, image, and word on the homepage is data you control from the admin. To sell something completely different, you don't touch code — you rebrand and replace content. This guide walks through it end to end.</p>\n\n<h3>How to use — step by step</h3>\n<ol>\n  <li><strong>Rename the store.</strong> Go to <em>Appearance &rarr; Branding</em>. Change <strong>Store name</strong> and the <strong>Announcement bar text</strong>, then click <strong>Save &amp; Apply</strong>. This name appears in the header, admin, and browser tab.</li>\n  <li><strong>Swap the logo &amp; favicon.</strong> Still on the Branding page, upload a <em>Logo (light bg)</em>, <em>Logo (dark bg)</em>, and a <strong>Favicon</strong>. Save &amp; Apply reloads the site with your identity.</li>\n  <li><strong>Replace categories.</strong> Open <em>Catalog &rarr; Categories</em>. Delete the fish categories and add your own (e.g. \"Dresses\", \"Laptops\", \"Fresh Produce\").</li>\n  <li><strong>Replace products.</strong> Open <em>Catalog &rarr; Products</em>, add your items with prices and descriptions, and attach images from the <em>Media Library</em>.</li>\n  <li><strong>Rewrite the homepage.</strong> Open <em>Content &rarr; Homepage</em>. Edit the hero heading, subtitle, buttons, stats, section headings, promo banners, testimonials, and newsletter copy. Use the <strong>Sections — show / hide</strong> panel to turn OFF anything that doesn't fit (for example <em>Fish masala</em> or <em>Recipes</em>).</li>\n  <li><strong>Change the theme.</strong> Open <em>Appearance &rarr; Theme &amp; Layout</em>, pick a preset or set your own colors, fonts, card surface, background, and hero layout. Save &amp; Apply updates the whole storefront.</li>\n  <li><strong>(Optional) Tune SEO.</strong> In <em>Configuration &rarr; SEO Engine</em>, set the site name and title template so search results match your new brand.</li>\n</ol>\n\n<h4>Concrete examples</h4>\n<p><strong>Fashion:</strong> categories become collections (Men, Women, Accessories), a warm theme preset, lifestyle hero image. <strong>Electronics:</strong> categories like Phones/Laptops, a cool blue theme, spec-heavy product descriptions. <strong>Grocery:</strong> categories by aisle, a fresh green theme, and the newsletter section kept on for weekly deals.</p>\n\n<h3>Tips</h3>\n<ul>\n  <li>Nothing is deleted permanently until you save — experiment freely, then <strong>Save &amp; Apply</strong>.</li>\n  <li>Hide (don't fear) leftover sections: the toggle grid lets you switch off any block instead of emptying it.</li>\n  <li>Upload images once to the <em>Media Library</em>; the hero picker and products both reuse them.</li>\n  <li>Set your <em>Favicon</em> early — it's the small icon in the browser tab that makes the store feel finished.</li>\n</ul>\n\n<h4>For developers</h4>\n<p>Content and settings are stored server-side, so rebranding needs no rebuild. The SEO title template uses replaceable tokens, and the Theme editor exposes a Custom CSS box that maps to CSS variables:</p>\n<pre><code>Title template:  %page% — %site%\nCustom CSS uses theme vars:\n  .container-x { max-width: 1360px; }\n  a { color: var(--primary); }</code></pre>\n<p>Seed defaults live in the server content files, but you never have to edit them — the admin UI writes the live config for you.</p>"
  },
  {
    "slug": "analytics",
    "title": "Analytics & visitors",
    "category": "Overview",
    "icon": "BarChart3",
    "summary": "See revenue, orders and traffic on the Dashboard and Analytics pages, plus a live visitor log and IP banning under Visitors & IPs.",
    "html": "<p>Banglarfish gives you three views of who is visiting and what they are buying: the <strong>Dashboard</strong> (a quick daily pulse), <strong>Analytics</strong> (deep sales and traffic reports), and <strong>Visitors &amp; IPs</strong> (a live visit log plus IP banning). Every number comes straight from your own store database, so there is no third-party tracker to set up.</p>\n\n<h3>What each screen shows</h3>\n<ul>\n<li><strong>Dashboard</strong> (the Admin home): KPI cards for total revenue, revenue today, orders and products, a 14-day revenue chart, quick-action buttons, recent orders and a low-stock list.</li>\n<li><strong>Analytics</strong> (<code>Admin → Analytics</code>): revenue and orders over time, growth versus the previous period, average order value, visits and unique visitors, top products, top customers, order and payment breakdowns, devices, countries, top pages and top referrers.</li>\n<li><strong>Visitors &amp; IPs</strong> (<code>Admin → Visitors</code>): a live visit log (time, IP, page, device, location, referrer) and the banned-IP list.</li>\n</ul>\n\n<h3>How to use it</h3>\n<ol>\n<li>Open <strong>Admin → Analytics</strong>.</li>\n<li>Pick a range at the top: <strong>7 days</strong>, <strong>30 days</strong>, <strong>90 days</strong> or <strong>1 year</strong>. Charts and tables reload for that window.</li>\n<li>Read the KPI cards. The green or red arrow compares this period against the previous period of the same length.</li>\n<li>Click <strong>Export CSV</strong> to download the whole report (summary, revenue by day, visits by day, top products, top customers, top pages and traffic sources) for the selected range.</li>\n<li>For traffic, go to <strong>Visitors &amp; IPs</strong> and switch between the <strong>Live visitors</strong> and <strong>Banned IPs</strong> tabs.</li>\n</ol>\n\n<h3>Banning an IP</h3>\n<ol>\n<li>On <strong>Live visitors</strong>, click <strong>Ban</strong> next to a row, or open the <strong>Banned IPs</strong> tab and type an IP plus an optional reason.</li>\n<li>The block takes effect immediately for every page and API request from that address.</li>\n<li>To lift it, click <strong>Unban</strong> on the Banned IPs tab.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Revenue excludes <em>cancelled</em> and <em>refunded</em> orders; the paid-vs-COD split shows how much cash was actually collected up front.</li>\n<li>Bot traffic is filtered out automatically, so visit counts stay realistic.</li>\n<li>City and country only appear when a geo lookup service is configured (<code>GEOIP_ENDPOINT</code>).</li>\n<li>Banning and unbanning need the <strong>manager</strong> role; viewing reports needs <strong>staff</strong>.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>These screens are backed by server functions in <code>src/lib/analytics.functions.ts</code>, writing to the <code>page_views</code> and <code>banned_ips</code> tables.</p>\n<pre><code>// Pull a report (days: 1–365)\nconst report = await adminAnalytics({ data: { days: 30 } });\n\n// Ban / unban an address (manager only)\nawait adminBanIp({ data: { ip: \"203.0.113.10\", reason: \"spam\" } });\nawait adminUnbanIp({ data: { ip: \"203.0.113.10\" } });</code></pre>"
  },
  {
    "slug": "products",
    "title": "Products",
    "category": "Catalog",
    "icon": "Package",
    "summary": "Create and edit catalog products — pricing, images, variants, stock, flags and SEO — from Admin → Products.",
    "html": "<p>The <strong>Products</strong> page (<code>Admin → Products</code>) is where you build your catalog. The table lists every product with its image, category, price, stock and status, plus a running count of how many are <em>low stock</em> (under 30 units). Use the search box and the category dropdown to narrow the list.</p>\n\n<h3>How to add or edit a product</h3>\n<ol>\n<li>Click <strong>Add product</strong> (or the pencil icon on a row) to open the editor.</li>\n<li>Enter a <strong>Name</strong> (required) and, optionally, a <strong>Bangla name</strong>.</li>\n<li>Leave <strong>Slug</strong> blank to auto-generate it from the name, or type your own. Slugs must be unique — they form the storefront URL.</li>\n<li>Pick a <strong>Category</strong>, and optionally set <strong>SKU</strong> and <strong>Brand</strong>.</li>\n<li>Set <strong>Price (৳)</strong> (required, whole taka). Add a higher <strong>Compare-at</strong> price to show a strikethrough \"was\" price.</li>\n<li>Enter <strong>Stock</strong>, <strong>Unit</strong> (e.g. <code>kg</code>) and <strong>Weight options</strong>.</li>\n<li>Write the <strong>Description</strong> in the rich-text editor, choose an image, and add variants, attributes and SEO fields as needed.</li>\n<li>Tick <strong>Active</strong> to show it on the storefront (unticked = Hidden), then <strong>Save product</strong>.</li>\n</ol>\n\n<h4>Variants, images and SEO</h4>\n<p><strong>Variants</strong> (e.g. <code>500g</code>, <code>1kg</code>) each carry their own price and stock. When variants exist they replace the weight options, and the variant price is authoritative at checkout — the base price is ignored. <strong>Images</strong> can be typed as a <code>/uploads/...</code> path or picked from the media grid; the first image is the main one. Under <strong>SEO</strong>, set a <strong>Meta title</strong>, <strong>Meta description</strong> and <strong>OG image</strong> to control how the product looks in search and social previews.</p>\n\n<h4>How stock decrements</h4>\n<p>When an order is placed, stock is reduced <strong>atomically</strong> using a conditional update, so two shoppers can't oversell the same unit. If nothing is left, the order is rejected and an inventory-ledger entry records the change:</p>\n<pre><code>UPDATE products SET stock = stock - qty\nWHERE id = :id AND stock >= qty;  -- 0 rows affected = sold out</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Prices are stored as whole taka (integers) — don't enter decimals.</li>\n<li>Use the <strong>Best seller</strong> and <strong>New arrival</strong> flags to feature products on the homepage.</li>\n<li>Add <strong>Attributes</strong> (Origin, Material, Color…) for a clean specs table on the product page.</li>\n<li>Watch the low-stock count in the header and restock before items hit zero.</li>\n<li>Keep slugs stable after launch — changing one breaks existing links and SEO.</li>\n</ul>"
  },
  {
    "slug": "categories",
    "title": "Categories",
    "category": "Catalog",
    "icon": "Tag",
    "summary": "Create and organize the product groups that power your storefront nav, shop filters, and homepage category grid.",
    "html": "<p>Categories are the groups that organize your catalog (for example, <em>Fish</em>, <em>Fish Masala</em>, or <em>Ready to Cook</em>). They appear in three places automatically: the storefront top navigation, the filter on the Shop page, and the category grid on the homepage. Set them up once and the whole store stays tidy.</p>\n\n<h3>What each field does</h3>\n<ul>\n<li><strong>Name</strong> — the label shoppers see (e.g. <em>Fish Masala</em>).</li>\n<li><strong>Slug</strong> — the URL-friendly version used in links like <code>/category/fish-masala</code>. Use lowercase letters and hyphens, no spaces.</li>\n<li><strong>Bengali / secondary name</strong> — an optional second-language label shown alongside the English name.</li>\n<li><strong>Image</strong> — the thumbnail used in the homepage grid and navigation.</li>\n<li><strong>Sort order</strong> — a number that controls position. Lower numbers appear first.</li>\n<li><strong>Active toggle</strong> — turn a category off to hide it everywhere without deleting it.</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Go to <strong>Admin → Categories</strong>.</li>\n<li>Click <strong>Add Category</strong> (or select an existing one to edit).</li>\n<li>Type the <strong>Name</strong>. The <strong>Slug</strong> is usually filled in for you — adjust it if needed.</li>\n<li>Add the <strong>Bengali / secondary name</strong> if you use dual-language labels.</li>\n<li>Upload an <strong>Image</strong> for the homepage grid.</li>\n<li>Set the <strong>Sort order</strong> to control where it sits in the nav and grid.</li>\n<li>Make sure the <strong>Active</strong> toggle is on, then <strong>Save</strong>.</li>\n</ol>\n\n<h3>The Fish Masala homepage band</h3>\n<p>The homepage has a dedicated masala band that pulls products from one specific category. By default this is the <strong>Fish Masala</strong> category, matched by its slug. The slug it looks for is configurable, so if you rename or restructure your masala category, point the band at the new slug in your homepage settings:</p>\n<pre><code>masala band → category slug: fish-masala</code></pre>\n<p>If the band looks empty, confirm the category exists, is <strong>Active</strong>, has products assigned, and that its slug matches the one configured for the band.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>Keep slugs stable. Changing a slug changes the category URL and can break existing links or bookmarks.</li>\n<li>Use sort order in steps of 10 (10, 20, 30) so you can insert new categories later without renumbering everything.</li>\n<li>Hide seasonal categories with the <strong>Active</strong> toggle instead of deleting them — you keep the setup for next time.</li>\n<li>Add a clear, similarly-sized <strong>Image</strong> to every category so the homepage grid stays even.</li>\n<li>Assign each product to the right category so it shows up under the Shop filter and in the nav.</li>\n</ul>"
  },
  {
    "slug": "media",
    "title": "Media Library",
    "category": "Catalog",
    "icon": "Image",
    "summary": "Upload, compress, edit, annotate and AI-generate images: automatic size reduction, crop/adjust, background removal, markup tools and Gemini/OpenAI generation.",
    "html": "<p>The <strong>Media Library</strong> (<code>Admin &rarr; Media Library</code>) is where every image in your store lives. Upload once, then reuse the same picture on products, categories, the homepage hero, banners and blog covers. Files are saved on the server and served from <code>/uploads</code>.</p>\n\n<h3>Uploading and reusing</h3>\n<ol>\n<li>Click <strong>Upload</strong> and pick one or more images (8&nbsp;MB max each). They appear at the top of the grid straight away.</li>\n<li>Already have images on the server from a previous install or a seed? Click <strong>Import existing</strong> to register them in the library.</li>\n<li>Hover a thumbnail for <strong>Edit</strong>, <strong>Copy URL</strong> and <strong>Delete</strong>.</li>\n<li>Give every image <strong>alt text</strong> &mdash; it is read by screen readers and used by Google. The sparkle button next to the field writes one for you.</li>\n</ol>\n\n<h3>Automatic compression</h3>\n<p>Every upload is compressed the moment it lands, the same way ShortPixel or TinyPNG would. <strong>The resolution is never changed</strong> &mdash; a 2000&times;2000 photo stays 2000&times;2000, it just weighs a fraction of what it did. JPEGs go through mozjpeg with progressive encoding and trellis quantisation, PNGs are palette-reduced at maximum effort, WebP is re-encoded at effort 6. If the compressed version somehow comes out bigger, the original is kept untouched.</p>\n<ul>\n<li>The bar at the top of the library shows how many files are compressed, how many are pending, and how much disk you have saved.</li>\n<li><strong>Compress images</strong> runs the whole library in small batches, with live progress. Safe to run repeatedly &mdash; anything already done is skipped.</li>\n<li>Originals are copied to <code>uploads/_originals</code> first, so nothing is destroyed and you can always go back.</li>\n</ul>\n\n<h3>The image editor</h3>\n<p>Click the wand on any thumbnail. The editor is pure canvas &mdash; nothing is uploaded until you press Save &mdash; and has five tabs:</p>\n<ul>\n<li><strong>Crop</strong> &mdash; eight drag handles, rule-of-thirds guides, aspect presets (1:1, 4:3, 16:9&hellip;), rotate in 90&deg; steps, free-angle rotation and horizontal/vertical flip.</li>\n<li><strong>Adjust</strong> &mdash; brightness, contrast, saturation, blur, a real unsharp-mask <em>sharpen</em>, and one-click looks (Vivid, Warm, Cool, B&amp;W, Fade).</li>\n<li><strong>Cut-out</strong> &mdash; removes the background behind the subject. It flood-fills inward from the edges, so a white plate <em>inside</em> the product survives and only the surrounding backdrop goes. <strong>Tolerance</strong> controls how much colour variation counts as background; <strong>Edge softness</strong> feathers the alpha so the cut-out doesn't look scissored. You can also replace the background with a solid colour in one click. Best on catalogue photography shot against a plain backdrop.</li>\n<li><strong>Markup</strong> &mdash; arrows, lines, boxes, circles, free text, pill <strong>badges</strong> (SALE, NEW, -20%) and a <strong>pixelate</strong> tool for hiding a competitor's label or a price tag. Pick a colour and size, drag on the image, undo one at a time or clear them all. Annotations are baked into the file when you save or download.</li>\n<li><strong>Export</strong> &mdash; output dimensions with an aspect lock and quick sizes, WebP/JPEG/PNG, a quality slider with Max / Balanced / Small presets, and a <strong>live estimate of the resulting file size</strong>. <strong>Download</strong> saves it to your computer; <strong>Save image</strong> adds it to the library as a new file (the original is left alone).</li>\n</ul>\n<p>The same editor is available wherever you pick an image &mdash; products, categories, the logo and favicon, banners and the landing-page builder.</p>\n\n<h3>AI images</h3>\n<p>Set an image provider under <code>Admin &rarr; AI Assistant &rarr; AI images</code> and two things unlock. This is configured separately from the text model, because Claude writes copy but cannot draw.</p>\n<ul>\n<li><strong>Google Gemini</strong> (<code>gemini-2.5-flash-image</code>) &mdash; generates <em>and</em> edits, so it powers both buttons. Key from <code>aistudio.google.com</code>.</li>\n<li><strong>OpenAI</strong> (<code>gpt-image-1</code>) &mdash; <code>/images/generations</code> and <code>/images/edits</code>. Key from <code>platform.openai.com</code>.</li>\n</ul>\n<ol>\n<li><strong>Generate image</strong> in the library toolbar turns a text description into a new image and files it straight into the library, compressed like any other upload. Describe the framing, background and lighting for the best result.</li>\n<li><strong>AI edit</strong> in the editor's Cut-out tab replaces the background, relights a product shot, or does anything else you can describe in a sentence. Use it when the instant cut-out struggles &mdash; hair, fur, glass, or a busy backdrop.</li>\n</ol>\n<p>Images are downscaled to 1536&nbsp;px on the long edge before being sent to the provider, so requests stay fast and inside the payload limit.</p>\n\n<h3>Tips</h3>\n<ul>\n<li><strong>Reuse, don't re-upload.</strong> One URL can be used in unlimited places.</li>\n<li><strong>Suggested sizes:</strong> hero and wide banners around <strong>1600&times;600</strong>; product and category images square near <strong>1000&times;1000</strong>; blog covers around <strong>1200&times;630</strong>.</li>\n<li><strong>WebP at Balanced quality</strong> is usually 60&ndash;80% smaller than the source JPEG with no visible difference. It is the right default for everything except images that need transparency &mdash; those need PNG or WebP.</li>\n<li><strong>Delete carefully.</strong> Removing an image breaks any page still pointing at that URL, so update those pages first.</li>\n</ul>\n\n<h4>For developers</h4>\n<p>Uploads are written to <code>UPLOAD_DIR</code> (default <code>public/uploads</code> in dev) and served by an application route, so they work whether or not nginx has an alias for the path. The file extension is derived from the <em>sniffed</em> magic bytes rather than the supplied filename, and identical files are de-duplicated by content hash.</p>\n<pre><code># .env\nUPLOAD_DIR=/var/www/banglarfish/uploads\nAI_IMAGE_PROVIDER=gemini      # none | gemini | openai\nAI_IMAGE_API_KEY=...\nAI_IMAGE_MODEL=gemini-2.5-flash-image\n\n# nginx\nclient_max_body_size 12m;</code></pre>\n<p>Compression lives in <code>src/server/image-optimizer.ts</code> (sharp; backup &rarr; encode &rarr; compare &rarr; atomic rename) and runs from <code>storeUpload()</code> on every new file. Background removal and annotation drawing are client-side canvas code in <code>src/components/admin/image-tools.ts</code>. Deleting a library item removes the database row but leaves the file on disk, so prune <code>UPLOAD_DIR</code> periodically if space matters.</p>"
  },
  {
    "slug": "orders",
    "title": "Orders",
    "category": "Sales",
    "icon": "ShoppingCart",
    "summary": "View every order, track its status through the fulfillment lifecycle, inspect customer and delivery details, and update status with full history.",
    "html": "<p>The <strong>Orders</strong> page (<code>Admin → Orders</code>) is your fulfillment hub. It lists every order pulled live from the database, newest first, with the customer, items, totals, payment, and current status. A running total of all order values is shown at the top.</p>\n\n<h3>The order lifecycle</h3>\n<p>Each order moves through a series of statuses. The typical happy path is:</p>\n<ol>\n<li><strong>pending</strong> — just placed, not yet reviewed</li>\n<li><strong>confirmed</strong> — you have accepted the order</li>\n<li><strong>processing</strong> — being prepared</li>\n<li><strong>packed</strong> — ready to hand to courier</li>\n<li><strong>shipped</strong> — out for delivery</li>\n<li><strong>delivered</strong> — received by the customer</li>\n</ol>\n<p>Two off-path statuses handle exceptions: <strong>cancelled</strong> (order stopped) and <strong>refunded</strong> (money returned). Payment is tracked separately as <em>pending</em>, <em>paid</em>, <em>failed</em>, or <em>refunded</em>.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Use the <strong>status tabs</strong> (All, pending, confirmed…) to filter; each shows a live count.</li>\n<li>Use the <strong>search box</strong> to find an order by order number, customer name, or phone.</li>\n<li>Change status quickly from the <strong>Status dropdown</strong> in each table row.</li>\n<li>Click the <strong>eye icon</strong> to open the order. Here you see delivery address, item list, subtotal, discount (and coupon), shipping, and total.</li>\n<li>Inside the order, update <strong>Status</strong> or <strong>Payment</strong> from their dropdowns, and use <strong>Print invoice</strong> to print or save a PDF.</li>\n<li>Scroll to <strong>History</strong> to see every status change with its timestamp.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Every status change is recorded in the order's <strong>History</strong>, so you always have an audit trail of who changed what and when.</li>\n<li>When you change the status, the customer is automatically sent an <strong>SMS update</strong> (if a phone number is on the order) — so only advance the status when it is truly true.</li>\n<li>Keep <strong>Payment status</strong> accurate for cash-on-delivery orders: mark them <em>paid</em> once you collect.</li>\n<li>Statuses are independent — you can jump straight to <em>cancelled</em> or <em>refunded</em> from any point.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Orders are served by TanStack server functions in <code>src/lib/admin-orders.functions.ts</code>, all guarded by <code>requireStaff()</code>. Status updates write a row to <code>orderStatusHistory</code> and an audit entry:</p>\n<pre><code>adminUpdateOrderStatus({\n  data: { id, status: \"shipped\", payment_status: \"paid\" }\n});\n// → updates orders, inserts orderStatusHistory,\n//   fires SMS via sendStatusUpdate, writes audit log</code></pre>"
  },
  {
    "slug": "coupons",
    "title": "Coupons",
    "category": "Sales",
    "icon": "Ticket",
    "summary": "Create percent or fixed discount codes with minimum subtotal, usage limits, and expiry — validated and applied automatically at checkout.",
    "html": "<p>Coupons let you offer discount codes that shoppers type at checkout. Manage them under <strong>Admin → Coupons</strong> (the \"Coupons &amp; Discounts\" page). Every coupon is checked and applied <em>on the server</em> during checkout, so a code can't be faked, reused past its limit, or applied when it doesn't qualify.</p>\n\n<h3>What each field does</h3>\n<ul>\n<li><strong>Code</strong> — what the shopper types (e.g. <code>EID20</code>). It's saved in UPPERCASE and matched case-insensitively.</li>\n<li><strong>Type</strong> — <em>Percentage</em> (e.g. 20% off) or <em>Fixed ৳</em> (a flat taka amount off).</li>\n<li><strong>Value</strong> — a whole number: the percent, or the taka amount. A fixed discount never exceeds the subtotal.</li>\n<li><strong>Min. subtotal (৳)</strong> — the cart must reach this before the code works. Use <code>0</code> for no minimum.</li>\n<li><strong>Usage limit</strong> — total redemptions allowed across all customers. Use <code>0</code> for unlimited.</li>\n<li><strong>Expires</strong> — last valid date; leave blank for never.</li>\n<li><strong>Active</strong> — the on/off switch. Untick to pause a code without deleting it.</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Go to <strong>Admin → Coupons</strong> and click <strong>Add coupon</strong>.</li>\n<li>Enter a short, memorable <strong>Code</strong>.</li>\n<li>Pick a <strong>Type</strong> and enter the <strong>Value</strong>.</li>\n<li>Optionally set a <strong>Min. subtotal</strong>, <strong>Usage limit</strong>, and <strong>Expiry</strong>.</li>\n<li>Make sure <strong>Active</strong> is ticked, then click <strong>Save</strong>.</li>\n<li>Share the code. The card shows a live <code>used / limit</code> bar so you can track redemptions.</li>\n</ol>\n<p>The discount applies to the <strong>subtotal only</strong> — shipping and tax are added afterward. One coupon can be used per order.</p>\n\n<h3>Tips for campaigns</h3>\n<ul>\n<li>Set a <strong>Min. subtotal</strong> to lift average order value (\"৳100 off orders over ৳1000\").</li>\n<li>Use a <strong>Usage limit</strong> for flash sales (\"first 50 orders\") to cap your cost.</li>\n<li>Always set an <strong>Expiry</strong> on seasonal codes so they don't linger.</li>\n<li>Pause a code instantly with the <strong>Active</strong> toggle instead of deleting it, so past-order history stays intact.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>At checkout the server accepts the code and applies it only if every rule passes:</p>\n<pre><code>active\n  &amp;&amp; (no expiresAt || expiresAt &gt; now)\n  &amp;&amp; subtotal &gt;= minSubtotal\n  &amp;&amp; (usageLimit === 0 || usage &lt; usageLimit)</code></pre>\n<p>The discount is then computed and <code>usage</code> is incremented inside the order transaction:</p>\n<pre><code>percent: round(subtotal * value / 100)\nfixed:   min(value, subtotal)   // never below zero</code></pre>\n<p>Because values are stored as whole taka integers, decimals (paisa) aren't supported.</p>"
  },
  {
    "slug": "shipping",
    "title": "Shipping zones",
    "category": "Sales",
    "icon": "Truck",
    "summary": "Set per-city delivery rates, free-shipping thresholds and ETAs, with a global flat-rate fallback for cities not in any zone.",
    "html": "<p>Shipping zones let you charge different delivery rates for different cities. When a customer checks out, the system looks at their city and picks the matching zone to decide the delivery charge and estimated arrival time. If no zone matches, it falls back to your global flat rate from <strong>Settings</strong>.</p>\n\n<h3>How it works</h3>\n<p>Each zone has a list of <strong>cities</strong>, a flat <strong>rate</strong>, a <strong>free above</strong> threshold, and an <strong>ETA</strong> label. At checkout the buyer's city is lowercased and matched against every active zone's cities. The first active zone that matches wins. Then:</p>\n<ul>\n<li>If the order total (after any coupon) is <strong>at or above</strong> the zone's <em>Free above</em> amount, shipping is <strong>৳0</strong>.</li>\n<li>Otherwise the buyer pays the zone's flat <em>Rate</em>.</li>\n</ul>\n<p>If the city matches no active zone, the system uses <strong>Settings → Free delivery threshold</strong> and <strong>Standard delivery charge</strong> (defaults ৳2000 and ৳80) instead.</p>\n\n<h3>Step by step</h3>\n<ol>\n<li>Go to <strong>Admin → Shipping</strong>.</li>\n<li>Click <strong>Add zone</strong>.</li>\n<li>Enter a <strong>Zone name</strong> (for your reference, e.g. <code>Inside Dhaka</code>).</li>\n<li>List the <strong>Cities</strong>, comma separated (e.g. <code>Dhaka, Gazipur, Narayanganj</code>).</li>\n<li>Set the <strong>Rate (৳)</strong> charged for this zone.</li>\n<li>Set <strong>Free above (৳)</strong> — the order total at which shipping becomes free.</li>\n<li>Write an <strong>ETA</strong> label shown to customers (e.g. <code>1–2 days</code>).</li>\n<li>Tick <strong>Active</strong> and click <strong>Save</strong>.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Matching is a substring check, so <code>Dhaka</code> also catches values like <code>Dhaka North</code>. Keep city names short and specific to avoid over-matching.</li>\n<li>Only <strong>Active</strong> zones are considered. Toggle a zone off to disable it without deleting it.</li>\n<li>Order zones from most specific to broadest — the first active match wins, so avoid overlapping city lists.</li>\n<li>To offer free shipping everywhere in a zone, set <em>Free above</em> to <code>0</code>.</li>\n<li>Always keep the global fallback in <strong>Settings</strong> sensible — it covers every city you have not zoned.</li>\n</ul>\n\n<h3>Shipping zones vs Delivery Areas</h3>\n<p>These are different features. <strong>Delivery Areas</strong> decide <em>whether</em> you deliver somewhere — an uncovered address is rejected at checkout. <strong>Shipping zones</strong> only decide the <em>price and ETA</em> for addresses you already deliver to.</p>\n\n<h4>For developers</h4>\n<p>Resolution happens server-side in <code>createOrder</code>. The rate is recomputed authoritatively — client-sent values are ignored:</p>\n<pre><code>const shipping = zone\n  ? afterDiscount >= zone.freeAbove ? 0 : zone.rate\n  : afterDiscount >= (settings.freeShippingThreshold ?? 2000)\n    ? 0 : (settings.standardShipping ?? 80);</code></pre>"
  },
  {
    "slug": "delivery-areas",
    "title": "Delivery Areas (coverage)",
    "category": "Sales",
    "icon": "MapPin",
    "summary": "Restrict where you deliver by matching addresses against covered areas, enforced at signup, on the account page, and at checkout.",
    "html": "<p>The <strong>Delivery Areas</strong> screen (<code>Admin &rarr; Delivery Areas</code>) controls where your shop will deliver. It has one master on/off switch, an \"out of area\" message, and a list of covered areas. When the restriction is on, any customer whose address falls outside your covered areas is blocked &mdash; at <strong>signup completion</strong>, on the <strong>account page</strong>, and at <strong>checkout</strong>. When it's off, you deliver everywhere and no address is ever blocked.</p>\n\n<h3>How coverage matching works</h3>\n<p>Each area has a list of <strong>matching terms</strong> &mdash; cities, thanas, area names, or postal codes. An address is considered covered if <em>any</em> term appears anywhere in its city, district, street line, or postal code. Matching is a simple, case-insensitive text search, so <code>dhaka</code> matches \"Dhaka\", \"DHAKA\", or \"North Dhaka\". If restriction is on and the customer leaves the address blank, they are treated as out of area.</p>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <code>Admin &rarr; Delivery Areas</code>.</li>\n<li>Turn on <strong>Restrict to covered areas</strong>. (Off means deliver everywhere.)</li>\n<li>Edit the <strong>\"Out of area\" message</strong> &mdash; this is exactly what customers see when their address isn't covered.</li>\n<li>Click <strong>Add area</strong>, give it a name (e.g. <em>Dhaka City</em>), and enter comma-separated matching terms like <code>dhaka, gulshan, banani, mirpur, 1212, 1213</code>.</li>\n<li>Add more areas as needed, then click <strong>Save</strong>.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Use <strong>specific terms</strong>. Because matching is substring-based, a very short term (like <code>12</code>) can accidentally match unrelated postal codes or street numbers.</li>\n<li>Include both area names <em>and</em> postal codes so you catch customers who fill in only one field.</li>\n<li>Test by entering a real out-of-area address on the account page &mdash; you should see your message and be blocked from saving.</li>\n<li>Leaving restriction off (the default) is the safe choice while you're still building your term list.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Coverage logic lives in <code>src/lib/delivery.ts</code> and is shared across all three enforcement points. It returns <code>true</code> (allowed) whenever coverage is disabled:</p>\n<pre><code>isAddressCovered(\n  { city, district, address_line1, postal_code },\n  deliveryConfig\n) // false =&gt; show the \"out of area\" message</code></pre>\n<p>The config shape is <code>{ enabled, message, areas: [{ id, name, terms[] }] }</code>. Enforcement runs server-side in <code>orders.functions.ts</code> (checkout) and <code>account.functions.ts</code> (address save), and client-side in <code>complete-profile.tsx</code> (signup), so the gate can't be bypassed by editing the page.</p>"
  },
  {
    "slug": "homepage-editor",
    "title": "Homepage editor",
    "category": "Content",
    "icon": "Home",
    "summary": "Edit every homepage section's copy, images and CTAs, and toggle each storefront band on or off from one screen.",
    "html": "<p>The Homepage editor (<strong>Admin → Homepage</strong>) controls every section of your storefront's front page from one screen. You edit the text, links and images for each band, and you decide which bands appear at all. Nothing changes on the live site until you press <strong>Save changes</strong> at the top.</p>\n\n<h3>What you can edit</h3>\n<ul>\n<li><strong>Sections — show / hide:</strong> checkboxes for Categories, How it works, Best sellers, Fish masala, Promo banners, New arrivals, Recipes, Blog, Testimonials and Newsletter. Unchecked = hidden from the homepage.</li>\n<li><strong>Hero:</strong> eyebrow, two heading lines (line 2 is the highlighted one), subtitle, a primary and secondary CTA (each with its own label and link), and a hero image picked from your Media library.</li>\n<li><strong>Hero stats:</strong> three value + label pairs (for example <code>500+</code> / <code>Happy customers</code>).</li>\n<li><strong>Trust bar features:</strong> pick an icon (truck, snowflake, shield or clock), a title and a short description.</li>\n<li><strong>Section headings:</strong> eyebrow + title for Categories, Best sellers, New arrivals and Testimonials.</li>\n<li><strong>Recipes, Blog &amp; Masala:</strong> eyebrow and title for each band, plus the <strong>Masala category slug</strong> that decides which products fill the masala row.</li>\n<li><strong>Promo banners</strong> (subtitle, title, link, primary/brand theme; add or remove), <strong>Testimonials</strong> (name, location, quote; add or remove) and the <strong>Newsletter</strong> (title, subtitle, button label).</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <strong>Admin → Homepage</strong>.</li>\n<li>In <strong>Sections — show / hide</strong>, tick only the bands you want visible.</li>\n<li>Fill in the Hero copy, then choose a <strong>Hero image</strong> from the dropdown (upload it first under Media if it isn't listed).</li>\n<li>Work down the remaining cards, editing text and adding/removing promo banners and testimonials as needed.</li>\n<li>Press <strong>Save changes</strong>. A confirmation appears and the storefront updates.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>CTA and banner links are plain paths, for example <code>/shop</code> or <code>/category/hilsa</code>.</li>\n<li>The masala row is driven by the category slug — it must match a real category:\n<pre><code>Masala category slug: fish-masala</code></pre></li>\n<li>Hiding a section here does not delete its content; re-tick the box to bring it back exactly as it was.</li>\n<li>The <em>arrangement</em> of the hero (split, centered, fullbleed, minimal, spotlight, diagonal, showcase, gradient) is a separate choice — set it in <strong>Theme &amp; Layout → Homepage hero layout</strong>, not here.</li>\n</ul>"
  },
  {
    "slug": "pages-cms",
    "title": "Pages (CMS)",
    "category": "Content",
    "icon": "FileText",
    "summary": "Create and manage custom content pages like About, Contact, or Terms, with a rich editor, SEO controls, and menu placement.",
    "html": "<p>The <strong>Pages</strong> tool (<em>Admin → Pages</em>) lets you build standalone content pages such as About, Contact, Shipping, or Terms — without touching code. Each page gets its own web address at <code>/pages/{slug}</code> and can be added to your header or footer menus.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Go to <strong>Admin → Pages</strong>. You'll see every page with its title, slug, nav placement, and status (Draft or Published).</li>\n<li>Click <strong>Add page</strong> (or the pencil icon to edit an existing one).</li>\n<li>Enter a <strong>Title</strong> (required). Leave <strong>Slug</strong> blank to auto-generate it from the title, or type your own.</li>\n<li>Write your content in the <strong>Body</strong> editor. Use the toolbar for headings, bold, lists, links, and images. Click the <strong>HTML</strong> button (top-right of the editor) to switch to raw HTML source, and <strong>Visual</strong> to switch back.</li>\n<li>Set <strong>Status</strong> to <em>Published</em> when ready (Draft pages stay hidden from visitors). Optionally pick a <strong>Template</strong> (Default, Full width, or Landing) and a <strong>Sort</strong> order.</li>\n<li>Fill in the <strong>SEO</strong> fields: Meta title (defaults to your page title), Meta description, and an OG image URL for social previews.</li>\n<li>Tick <strong>Show in header</strong> or <strong>Show in footer</strong> to add the page to those menus. Tick <strong>No index</strong> to hide the page from search engines.</li>\n<li>Click <strong>Save page</strong>. Use the external-link icon in the list to preview it live.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Slugs become the URL, so keep them short and readable, e.g. <code>/pages/about-us</code>.</li>\n<li>Keep a page as <em>Draft</em> while you work on it — nothing goes public until it's Published.</li>\n<li>Use <strong>No index</strong> for pages like \"Thank you\" or internal notices you don't want in Google.</li>\n<li>Leave Meta title empty and the system reuses the page title automatically.</li>\n<li>Insert images by their URL (for example <code>/uploads/…</code>) using the image button in the toolbar.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Slugs are normalized when saved — lowercased, with non-alphanumeric runs collapsed to hyphens:</p>\n<pre><code>\"About Us!\" → \"about-us\"\ntitle.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\")</code></pre>\n<p>Body HTML is passed through a sanitizer before rendering at <code>/pages/{slug}</code>, so unsafe tags are stripped. When <code>noindex</code> is set, a <code>&lt;meta name=\"robots\" content=\"noindex\"&gt;</code> tag is emitted; <code>ogImage</code> and <code>metaDescription</code> populate the corresponding meta tags.</p>"
  },
  {
    "slug": "blog",
    "title": "Blog",
    "category": "Content",
    "icon": "Newspaper",
    "summary": "Write and manage blog articles — with cover images, rich content, tags and SEO — shown at /blog and in the homepage Blog band.",
    "html": "<p>The <strong>Blog</strong> section (Admin → Blog) is where you write articles like sourcing stories, buying guides and news. Published posts appear on your public <code>/blog</code> page and, if enabled, in the <strong>Blog band</strong> on the homepage. Each post also gets its own page at <code>/blog/your-slug</code>.</p>\n\n<h3>Step by step: publish a post</h3>\n<ol>\n<li>Click <strong>Add post</strong> (top right). To change an existing one, click the <strong>pencil</strong> icon on its row.</li>\n<li>Enter a <strong>Title</strong> (required). Leave <strong>Slug</strong> blank to auto-generate it from the title, or type your own (this becomes the URL, e.g. <code>/blog/hilsa-buying-guide</code>).</li>\n<li>Set the <strong>Author</strong> and choose a <strong>Status</strong>: <em>Draft</em> (hidden) or <em>Published</em> (live).</li>\n<li>Add a <strong>Category</strong> and <strong>Tags</strong> (comma-separated, e.g. <code>health, tips</code>).</li>\n<li>Paste a <strong>Cover image URL</strong> and write a short <strong>Excerpt</strong> — the excerpt is the preview text shown on cards.</li>\n<li>Write the <strong>Body</strong> using the rich text editor (headings, bold, links, lists, images).</li>\n<li>Fill in the <strong>SEO</strong> fields — Meta title, Meta description and OG image URL — to control how the post looks in Google and social shares.</li>\n<li>Click <strong>Save post</strong>. Use the <strong>external-link</strong> icon on the row to preview it live.</li>\n</ol>\n\n<h3>Recipes are separate</h3>\n<p>Any post whose <strong>Category starts with the word \"Recipe\"</strong> (case-insensitive) is treated as a recipe and shown in the <strong>Recipes</strong> section instead of the Blog. So a post categorised <code>Recipe</code> or <code>Recipe Ideas</code> will <em>not</em> appear at <code>/blog</code>. Use any other category name to keep a post in the Blog.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>Only <strong>Published</strong> posts are visible to visitors — drafts stay private until you switch them.</li>\n<li>Posts are ordered <strong>newest first</strong> by publish date across both the Blog page and homepage band.</li>\n<li>The homepage Blog band shows up to <strong>3</strong> recent posts and only appears when the band is enabled in <strong>Homepage</strong> settings and at least one non-recipe post is published.</li>\n<li>Keep excerpts to one or two sentences — long text is clamped on cards.</li>\n<li>Use the <strong>Search</strong> box in the admin list to find posts by title, slug, author, excerpt or category.</li>\n<li>Always add a Cover image; cards without one look plain and get less engagement.</li>\n</ul>\n\n<p><em>Developer note:</em> the recipe rule is a simple prefix match on the category:</p>\n<pre><code>const isRecipe = (c) => /^recipe/i.test((c ?? \"\").trim());\n// listBlog()  → posts where !isRecipe(category)\n// listRecipes() → posts where isRecipe(category)</code></pre>"
  },
  {
    "slug": "recipes",
    "title": "Recipes",
    "category": "Content",
    "icon": "ChefHat",
    "summary": "Publish cooking recipes with cover, description, ingredients and steps that appear at /recipes and in the homepage Recipes band.",
    "html": "<p>The <strong>Recipes</strong> section lets you publish cooking recipes — a cover photo, a short description, and the ingredients and steps written in the rich editor. Published recipes appear on your storefront at <code>/recipes</code> and, when enabled, in the <strong>Recipes band</strong> on your homepage. Under the hood, recipes reuse the blog system: each recipe is simply a blog post locked to the <em>Recipe</em> category, so you get the same editor without the extra fields getting in your way.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Open <strong>Admin → Recipes</strong> and click <strong>Add recipe</strong>.</li>\n<li>Enter a <strong>Title</strong>. Leave <strong>Slug</strong> blank to auto-generate it from the title, or type your own (used in the web address).</li>\n<li>Optionally set an <strong>Author</strong> and paste a <strong>Cover image URL</strong> (this is the photo shown on cards).</li>\n<li>Add <strong>Tags</strong> as a comma-separated list — this is where cook time and difficulty go, e.g. <code>30 min, Easy, Spicy</code>. The first three show as pills on the storefront.</li>\n<li>Write a one- or two-line <strong>Short description</strong> (the excerpt shown under each card).</li>\n<li>In the <strong>Recipe</strong> rich editor, write your <strong>ingredients and steps</strong> — use bullet lists for ingredients and a numbered list for the method.</li>\n<li>Set <strong>Status</strong> to <strong>Published</strong> and click <strong>Save recipe</strong>. Keep it <em>Draft</em> to hide it while you work.</li>\n</ol>\n\n<p>To review a live recipe, click the <strong>View</strong> (open) icon in the list. Recipe detail pages render at <code>/blog/&lt;slug&gt;</code>, even though the listing lives at <code>/recipes</code>.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>Only <strong>Published</strong> recipes appear on the storefront — drafts are always hidden from visitors.</li>\n<li>Recipes are kept separate from the Blog, so a recipe never shows up in the blog list, and vice versa.</li>\n<li>The homepage Recipes band shows your <strong>3 most recent</strong> published recipes, but only if the Recipes section is turned on in <strong>Admin → Homepage</strong> and at least one recipe exists.</li>\n<li>Add a cover image — cards without one fall back to a plain placeholder.</li>\n<li>Use the search box at the top of the list to find recipes by title, slug, author, or description.</li>\n</ul>\n\n<h4>For developers</h4>\n<p>A recipe is a blog post whose category matches this test; the admin form always saves the exact string <code>\"Recipe\"</code> for you.</p>\n<pre><code>const isRecipe = (c) =&gt; /^recipe/i.test((c ?? \"\").trim());\n// storefront data comes from listRecipes() — published posts, newest first</code></pre>"
  },
  {
    "slug": "banners",
    "title": "Banners",
    "category": "Content",
    "icon": "GalleryHorizontal",
    "summary": "Create promotional banners with an image, link, placement, sort order, and active toggle, and control where they appear on your storefront.",
    "html": "<p>Banners are the promotional images that headline your storefront — the big hero image at the top of the homepage, mid-page promo cards, and thin call-out strips. Manage them all under <strong>Admin &rarr; Banners</strong> (the page is titled <em>Banners &amp; Sliders</em>). Each banner is a small record: an image, some text, a link, where it should show, and whether it's live.</p>\n\n<h4>What each field does</h4>\n<ul>\n<li><strong>Title</strong> (required) — the headline/label for the banner.</li>\n<li><strong>Subtitle</strong> — an optional supporting line.</li>\n<li><strong>Image URL</strong> — paste a hosted image address. A live preview appears below the field so you can confirm it loads. This field takes a URL; it is not an uploader.</li>\n<li><strong>Link (href)</strong> — where a click goes, e.g. <code>/category/hilsa</code> or a full <code>https://</code> URL.</li>\n<li><strong>Placement</strong> — <code>hero</code>, <code>promo</code>, or <code>strip</code>. This tells the storefront where the banner belongs.</li>\n<li><strong>Sort</strong> — a number; lower values show first within the same placement.</li>\n<li><strong>Active</strong> — uncheck to hide a banner without deleting it.</li>\n</ul>\n\n<h4>How to add a banner</h4>\n<ol>\n<li>Go to <strong>Admin &rarr; Banners</strong> and click <strong>Add banner</strong>.</li>\n<li>Enter a <strong>Title</strong> (required) and, if you want, a <strong>Subtitle</strong>.</li>\n<li>Paste an <strong>Image URL</strong> and check the preview.</li>\n<li>Set the <strong>Link (href)</strong> so clicks land on the right page.</li>\n<li>Choose a <strong>Placement</strong> and a <strong>Sort</strong> number.</li>\n<li>Leave <strong>Active</strong> checked, then <strong>Save</strong>.</li>\n</ol>\n\n<h4>Where banners appear</h4>\n<p>The storefront reads only <strong>active</strong> banners, ordered by <strong>Sort</strong> ascending, and groups them by <strong>Placement</strong>: <em>hero</em> for the top of the homepage, <em>promo</em> for the mid-page promo area, and <em>strip</em> for a thin full-width bar. Note that your active Theme and Homepage settings also drive the visible hero/promo areas, so the exact position depends on your theme template.</p>\n\n<h4>Sizing tips</h4>\n<ul>\n<li>Use one consistent size per placement so banners line up. Hero images work well wide and landscape (e.g. ~1600&times;600); strips should be short and wide.</li>\n<li>Keep key wording in the <strong>Title</strong>/<strong>Subtitle</strong> rather than baked into the image — it stays readable on mobile.</li>\n<li>Compress images and serve them over HTTPS; large files slow the homepage.</li>\n<li>Use <strong>Sort</strong> to reorder, and toggle <strong>Active</strong> for seasonal campaigns instead of deleting.</li>\n</ul>\n\n<h4>For developers</h4>\n<p>The public server function <code>listBanners()</code> returns active rows sorted ascending; filter by <code>placement</code> in your template. Admin writes go through <code>adminUpsertBanner</code> / <code>adminDeleteBanner</code> and require staff auth.</p>\n<pre><code>const banners = await listBanners();\nconst hero  = banners.filter((b) =&gt; b.placement === \"hero\");\nconst promo = banners.filter((b) =&gt; b.placement === \"promo\");\nconst strip = banners.filter((b) =&gt; b.placement === \"strip\");</code></pre>"
  },
  {
    "slug": "menus",
    "title": "Menus (header & footer)",
    "category": "Content",
    "icon": "ListTree",
    "summary": "Build your header and footer navigation by adding, reordering, and editing simple label-and-link menu items.",
    "html": "<p>The <strong>Menu Builder</strong> (<code>Admin → Menus</code>) controls the navigation links in your site's top header bar and its footer. You manage two independent lists side by side: <strong>Header navigation</strong> and <strong>Footer navigation</strong>. Each menu item is just two things: a <strong>Label</strong> (the text shoppers see) and a <strong>URL</strong> (where it goes).</p>\n\n<h3>What appears automatically</h3>\n<p>You do <em>not</em> need to add your core pages by hand. The header already shows <strong>Home</strong>, <strong>Shop All</strong>, your <strong>Categories</strong>, <strong>Recipes</strong>, and <strong>Blog</strong> automatically (Recipes and Blog only show if those sections are enabled). The Menu Builder is for the <em>extra</em> custom links you want to add, such as an About or Contact page. Your custom header links appear after the built-in ones in the top navigation bar.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Go to <code>Admin → Menus</code>. You'll see the Header and Footer panels.</li>\n<li>Click <strong>Add link</strong> to create a new empty row.</li>\n<li>Type the <strong>Label</strong> (e.g. <code>About Us</code>) and the <strong>URL</strong> (e.g. <code>/about</code> for an internal page, or <code>https://…</code> for an external site).</li>\n<li>Drag the handle (the grip icon on the left) up or down to <strong>reorder</strong> items.</li>\n<li>Click the trash icon to <strong>remove</strong> a link.</li>\n<li>Click <strong>Save</strong> on that panel. Header and footer are saved <em>separately</em>, so save each one after editing it.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Internal links should start with <code>/</code> (for example <code>/shop</code> or <code>/blog</code>). External links need the full <code>https://</code> address.</li>\n<li>Empty rows (no label and no URL) are dropped automatically when you save, so blanks won't clutter your menu.</li>\n<li>If you add a link that points to <code>/blog</code> or <code>/recipes</code>, it is <strong>auto-hidden</strong> in the header to avoid duplicating the built-in items — those pages are already linked for you.</li>\n<li>Keep labels short so the navigation bar stays tidy on smaller screens.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Each menu is stored as an array of items with this shape:</p>\n<pre><code>type MenuItem = { label: string; href: string };\n// saved per location: \"header\" | \"footer\"</code></pre>\n<p>The header dedupe rule that hides Blog/Recipes duplicates is a simple href test:</p>\n<pre><code>headerLinks = menus.header.filter(\n  (l) =&gt; !/\\/(blog|recipes)\\/?$/i.test(l.href)\n);</code></pre>"
  },
  {
    "slug": "reviews",
    "title": "Reviews",
    "category": "Content",
    "icon": "MessageSquare",
    "summary": "Moderate customer product reviews (pending, approved, rejected); approved reviews auto-update each product's star rating.",
    "html": "<p>The <strong>Reviews</strong> page (<code>Admin → Reviews</code>) is where you moderate the reviews customers leave on your products. Signed-in customers post a rating, title, and comment from a product's <em>Reviews</em> tab. Every new review arrives as <strong>pending</strong> and stays hidden from your storefront until you approve it, so nothing goes public without your say-so.</p>\n\n<h3>How moderation works</h3>\n<p>Each review has one of three states: <strong>Pending</strong> (waiting for you), <strong>Approved</strong> (live on the product page), or <strong>Rejected</strong> (kept in your records but hidden). Only <strong>approved</strong> reviews appear on the storefront, and only approved reviews count toward a product's star rating and its \"(N reviews)\" total. The rating is recalculated automatically the moment you approve or reject something.</p>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <code>Admin → Reviews</code>. The header shows the total count and how many are <strong>pending moderation</strong>.</li>\n<li>Use the filter tabs — <strong>All</strong>, <strong>Pending</strong>, <strong>Approved</strong>, <strong>Rejected</strong> — to focus. Start with Pending.</li>\n<li>Read the card: customer name, product, star rating, date, title, and body.</li>\n<li>Click the green <strong>check</strong> to approve, or the amber <strong>X</strong> to reject. The status badge updates instantly.</li>\n<li>Use the red <strong>trash</strong> icon to delete a review permanently (you will be asked to confirm).</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Approve genuine feedback even when it is critical — a mix of ratings builds trust. Reject spam, abuse, or off-topic content instead of deleting it, so you keep a record.</li>\n<li>To pull an already-approved review back out of a product's average, use <strong>Reject</strong>. Rejecting recomputes the rating; <strong>Delete</strong> does not, so a deleted approved review can leave the star average slightly stale until the next approve/reject on that product.</li>\n<li>Approving fake five-star reviews inflates ratings and can violate consumer rules — moderate honestly.</li>\n<li>Staff, managers, and admins can all moderate reviews.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Approving/rejecting runs in <code>adminSetReviewStatus</code>. After updating the row it recomputes the product's rating from <em>approved</em> reviews only:</p>\n<pre><code>rating       = AVG(reviews.rating) WHERE status = 'approved'\nreviewsCount = COUNT(*)            WHERE status = 'approved'\n// written to products.rating / products.reviewsCount</code></pre>\n<p>New reviews are inserted with <code>status: \"pending\"</code>; the public product loader filters to <code>status = \"approved\"</code> ordered newest first.</p>"
  },
  {
    "slug": "theme-layout",
    "title": "Theme & Layout",
    "category": "Appearance",
    "icon": "Palette",
    "summary": "Restyle your entire storefront from one screen — pick a preset theme, then fine-tune colors, surfaces, backgrounds, fonts, and effects with live preview.",
    "html": "<p><strong>Theme &amp; Layout</strong> controls how your whole storefront looks — colors, card styles, backgrounds, fonts, buttons, and the homepage hero. The fastest way to a great look is to start from a preset, then tweak. Everything updates in the <strong>live preview</strong> as you go, so nothing changes for shoppers until you save.</p>\n\n<h3>How to use</h3>\n<ol>\n<li><strong>Pick a preset.</strong> Browse the 20+ built-in themes (use the category filter to narrow them down) and click one. This sets a full, coordinated look in one step — perfect if you're just starting out.</li>\n<li><strong>Set your colors.</strong> Adjust <em>primary</em>, <em>brand</em>, <em>accent 2</em>, <em>background</em>, <em>card</em>, and <em>text</em>. Primary/brand drive buttons and highlights; card and background set the page canvas.</li>\n<li><strong>Choose a card surface.</strong> Pick one of 8 styles — <code>elevated</code>, <code>glass</code>, <code>aurora</code>, <code>neo</code>, <code>clay</code>, <code>glossy</code>, <code>outline</code>, or <code>flat</code> — to define how product cards feel.</li>\n<li><strong>Tune the depth sliders.</strong> Use <em>depth</em>, <em>glass blur</em>, <em>opacity</em>, and <em>glow</em> to shape the real layered 3D shadows. Small moves go a long way.</li>\n<li><strong>Set a background.</strong> Choose from 9 options (some animated) to match your brand's energy.</li>\n<li><strong>Adjust typography.</strong> Pick a <em>body</em> font, a <em>heading</em> font, and a <em>scale</em> to size text up or down.</li>\n<li><strong>Toggle effects.</strong> Enable or disable gradient buttons, pill buttons, 3D hover, animations, and dark mode.</li>\n<li><strong>Choose a hero layout.</strong> Select one of 8 homepage hero layouts for the top of your storefront.</li>\n<li><strong>Save</strong> when the live preview looks right.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Change one thing at a time and watch the preview — it's easy to undo a single tweak.</li>\n<li>Keep <em>text</em> and <em>background</em> high-contrast so content stays readable.</li>\n<li>Use <strong>Export theme</strong> to save your look as JSON before big experiments; <strong>Import theme</strong> restores it or copies a look between stores.</li>\n<li>Animated backgrounds and heavy glow look striking but can distract from products — go lighter on storefronts with lots of items.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>The <strong>Custom CSS</strong> box (advanced) lets you override anything the presets don't cover. It's applied on top of the generated theme, so target the storefront's classes directly:</p>\n<pre><code>/* Tighten card corners and add a custom accent */\n.product-card { border-radius: 6px; }\n:root { --accent2: #ff6b35; }</code></pre>\n<p>Exported theme JSON captures every setting above, making it easy to version-control a look or replicate it across deployments.</p>"
  },
  {
    "slug": "branding",
    "title": "Branding",
    "category": "Appearance",
    "icon": "Sparkles",
    "summary": "Set your store name, light/dark logos, favicon, and announcement bar so your identity shows across the storefront, admin, and browser tab.",
    "html": "<p>The <strong>Branding</strong> screen (<strong>Admin → Branding</strong>) controls your store's identity: its name, logos, favicon, and the announcement bar. Anything you change here flows straight to the storefront header and the browser tab, so it's the fastest way to make the shop feel like <em>yours</em>.</p>\n\n<h3>What each field does</h3>\n<ul>\n<li><strong>Store name</strong> — used as your logo's alt text and as your store's identity across the site.</li>\n<li><strong>Announcement bar text</strong> — the thin promo strip at the very top of every page (e.g. a delivery offer).</li>\n<li><strong>Logo (light bg)</strong> — shown in the main header on light backgrounds.</li>\n<li><strong>Logo (dark bg)</strong> — used where the background is dark, so your logo stays readable.</li>\n<li><strong>Favicon</strong> — the tiny icon in the browser tab and bookmarks.</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Go to <strong>Admin → Branding</strong>.</li>\n<li>Type your <strong>Store name</strong> and your <strong>Announcement bar text</strong>.</li>\n<li>For each logo and the favicon, click <strong>Upload</strong> and pick an image file. A preview appears against a matching background (white for the light logo, dark for the dark logo).</li>\n<li>Prefer to host the file yourself? Paste a path or URL directly into the small text box under each image instead of uploading.</li>\n<li>Click <strong>Save &amp; Apply</strong>. The page reloads automatically so you see the new branding live.</li>\n</ol>\n\n<h3>How it reaches your visitors</h3>\n<p>The header logo is rendered by the <code>Logo</code> component, which reads your saved branding and falls back to bundled defaults if a field is empty. The favicon is injected into the page <code>&lt;head&gt;</code> as the tab icon. The announcement text renders in the top bar, and empty fields fall back to the built-in defaults.</p>\n\n<pre><code>// Fallback behaviour (simplified)\nlogo   = branding.logoLight || \"/img/logo-light.png\"\nfavicon = branding.favicon  || \"/img/favicon.png\"</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Use a <strong>transparent PNG or SVG</strong> for logos so they sit cleanly on any header colour.</li>\n<li>Upload <em>both</em> light and dark logos — leaving one blank falls back to a generic default, not your brand.</li>\n<li>Favicons look best as a square image (e.g. 32×32 or 64×64); the preview is small on purpose.</li>\n<li>Leave the announcement bar text empty if you don't want a custom promo — the default message shows instead.</li>\n<li>Changes are global and instant after <strong>Save &amp; Apply</strong>, so double-check spelling before saving.</li>\n</ul>"
  },
  {
    "slug": "custom-code",
    "title": "Custom Code (CSS / HTML / JS)",
    "category": "Appearance",
    "icon": "Code",
    "summary": "Add global CSS, head HTML, and footer HTML/JS to your storefront for styling, analytics, fonts, chat widgets, and tracking pixels.",
    "html": "<p>The <strong>Custom Code</strong> page (Admin &rarr; Custom Code) lets you inject your own code into every page of your storefront, WordPress-style, without touching any files. It has three sections: <strong>Custom CSS</strong> for styling, <strong>Head HTML</strong> for tags that belong in the page <code>&lt;head&gt;</code>, and <strong>Footer HTML/JS</strong> for scripts that load at the bottom of the page. Everything you save here is applied site-wide to the storefront your customers see.</p>\n\n<p><em>This affects the storefront only.</em> It never changes your server, database, or admin code, so you cannot break the backend from here. Only users with the <strong>Manager</strong> role or higher can edit these fields.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Go to <strong>Admin &rarr; Custom Code</strong>.</li>\n<li>Pick the right box: <strong>Custom CSS</strong> for looks, <strong>Head HTML</strong> for meta tags/fonts/analytics, or <strong>Footer HTML/JS</strong> for chat widgets and pixels.</li>\n<li>Paste or type your code into the field.</li>\n<li>Click <strong>Save</strong>, then open your storefront in a new tab and refresh to confirm it works.</li>\n</ol>\n\n<h4>Custom CSS</h4>\n<p>Reuse the site's theme colors with CSS variables so your changes match the design and adapt if the theme changes:</p>\n<pre><code>.sale-badge {\n  background: var(--primary);\n  color: var(--primary-foreground);\n  border-radius: 6px;\n}</code></pre>\n\n<h4>Head HTML</h4>\n<p>Good for verification meta tags, custom fonts, and analytics snippets:</p>\n<pre><code>&lt;meta name=\"google-site-verification\" content=\"YOUR_CODE\" /&gt;\n&lt;link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" /&gt;</code></pre>\n\n<h4>Footer HTML/JS</h4>\n<p>Best place for chat widgets and tracking pixels, since they load after the page content:</p>\n<pre><code>&lt;script&gt;\n  // paste your chat or pixel snippet here\n&lt;/script&gt;</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Change one thing at a time and refresh, so it is easy to see what caused an issue.</li>\n<li>Only paste code from sources you trust &mdash; scripts run on your customers' browsers.</li>\n<li>Prefer <code>var(--primary)</code> and other theme variables over hard-coded hex colors.</li>\n<li>Put third-party <code>&lt;script&gt;</code> tags in <strong>Footer</strong>, not <strong>Head</strong>, so they don't slow down page loading.</li>\n<li>Keep a copy of your snippets in a text file as a backup before making big edits.</li>\n<li>If the storefront looks broken, clear the field you last edited and save to instantly roll back.</li>\n</ul>"
  },
  {
    "slug": "payments",
    "title": "Payment gateways",
    "category": "Configuration",
    "icon": "CreditCard",
    "summary": "Enable and configure bKash, Nagad, SSLCommerz, and Cash on Delivery, and safely test checkout before taking real payments.",
    "html": "<p>The <strong>Payment Gateways</strong> screen (<code>Admin → Payment Gateways</code>) is where you decide how customers pay you. It supports four options built for Bangladesh: <strong>bKash</strong>, <strong>Nagad</strong>, <strong>SSLCommerz</strong> (cards and MFS aggregator), and <strong>Cash on Delivery</strong>. Each gateway has its own enable toggle and credential fields, and one shared <em>mode</em> switch controls the whole checkout. Credentials are stored in your database and used automatically at checkout.</p>\n\n<h3>Understanding the mode switch</h3>\n<p>At the top of the page you pick one mode for the entire store:</p>\n<ul>\n<li><strong>Simulate</strong> — a mock gateway. No credentials, no real money. Ideal for learning the full checkout flow.</li>\n<li><strong>Sandbox</strong> — connects to each provider's test endpoints using the sandbox credentials you enter below.</li>\n<li><strong>Live</strong> — real money is collected. Only switch here once sandbox works and credentials are correct.</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <code>Admin → Payment Gateways</code> and set the mode to <strong>Simulate</strong>.</li>\n<li>Turn on <strong>Enabled</strong> for each gateway you want to offer.</li>\n<li>For <strong>Cash on Delivery</strong>, write the <em>Instructions shown to customer</em> (for example, \"Pay the rider in cash on arrival\").</li>\n<li>Place a test order on your storefront to confirm the flow works end to end.</li>\n<li>Sign up with each provider and collect API credentials (see below), then switch mode to <strong>Sandbox</strong> and enter them.</li>\n<li>Test again in sandbox. When everything succeeds, switch to <strong>Live</strong>.</li>\n<li>Click <strong>Save</strong> (top right) after any change — nothing is stored until you save.</li>\n</ol>\n\n<h3>Where to get API credentials</h3>\n<ul>\n<li><strong>bKash (Tokenized Checkout)</strong>: <em>App Key</em>, <em>App Secret</em>, <em>Username</em>, <em>Password</em> — from your merchant account at <a href=\"https://developer.bka.sh\">developer.bka.sh</a>.</li>\n<li><strong>Nagad</strong>: <em>Merchant ID</em>, <em>Merchant Private Key</em>, <em>Nagad PG Public Key</em> — issued by Nagad when your merchant account is approved.</li>\n<li><strong>SSLCommerz</strong>: <em>Store ID</em> and <em>Store Password</em> — from your panel at <a href=\"https://developer.sslcommerz.com\">developer.sslcommerz.com</a>.</li>\n</ul>\n\n<h3>Tips</h3>\n<ul>\n<li>Always test in <strong>Simulate</strong> first, then <strong>Sandbox</strong>, and only then go <strong>Live</strong>.</li>\n<li>The mode is global — you cannot mix live bKash with sandbox Nagad.</li>\n<li>Cash on Delivery needs no credentials, so it works in any mode.</li>\n<li>Secret fields (secrets, passwords, keys) are masked; re-enter them if you rotate credentials.</li>\n<li>Keep at least one gateway enabled, or customers cannot complete checkout.</li>\n</ul>\n\n<p>Developer note: the config shape saved to the database looks like this:</p>\n<pre><code>{\n  \"mode\": \"sandbox\",\n  \"cod\":        { \"enabled\": true,  \"instructions\": \"...\" },\n  \"bkash\":      { \"enabled\": true,  \"appKey\": \"\", \"appSecret\": \"\", \"username\": \"\", \"password\": \"\" },\n  \"nagad\":      { \"enabled\": false, \"merchantId\": \"\", \"merchantPrivateKey\": \"\", \"pgPublicKey\": \"\" },\n  \"sslcommerz\": { \"enabled\": false, \"storeId\": \"\", \"storePasswd\": \"\" }\n}</code></pre>"
  },
  {
    "slug": "sms",
    "title": "SMS gateway (OTP)",
    "category": "Configuration",
    "icon": "Smartphone",
    "summary": "Configure Boomcast or any HTTP SMS API for phone-verification OTP and order alerts, with dev-mode logging and a test send.",
    "html": "<p>The <strong>SMS Gateway</strong> page (<code>Admin → SMS Gateway</code>) connects your store to an SMS provider so it can send phone-verification <strong>OTP</strong> codes at signup and order notifications. You can use the built-in <strong>Boomcast</strong> preset or wire up any <strong>Custom HTTP</strong> SMS API by mapping its parameter names. Codes are 6 digits, expire in 5 minutes, and allow up to 5 attempts.</p>\n\n<h3>How the request works</h3>\n<p>When a code is sent, the system builds a request from your settings: the API key, sender ID, recipient number, and message are placed into named parameters and sent to your API URL. For <code>GET</code> they become query-string values; for <code>POST</code> they become a form body (or JSON if you set the content-type to <code>application/json</code>). Bangladeshi numbers are auto-normalized to the <code>8801XXXXXXXXX</code> format.</p>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <code>Admin → SMS Gateway</code>.</li>\n<li>Pick a <strong>Provider</strong>: Boomcast (prefilled defaults) or Custom HTTP.</li>\n<li>Fill in <strong>Sender ID / Masking</strong>, <strong>API URL</strong>, <strong>HTTP Method</strong>, <strong>API Key</strong>, and (if required) <strong>Secret Key</strong>.</li>\n<li>If your provider expects different field names, open <strong>Advanced: request parameter names</strong> and rename each param to match your provider's docs.</li>\n<li>Untick <strong>Developer mode</strong> to send real messages (leave it on while testing).</li>\n<li>Click <strong>Save</strong>.</li>\n<li>Enter a number under <strong>Send a test SMS</strong> and click <strong>Send test</strong>. Save first — the test uses saved settings.</li>\n</ol>\n\n<h3>Mapping params to your provider</h3>\n<p>The Boomcast defaults map to: API key <code>apikey</code>, secret <code>secretkey</code>, sender <code>callerID</code>, recipient <code>toUser</code>, message <code>messageContent</code>. For another provider, look up its send-SMS endpoint and copy its exact field names into the matching Advanced fields.</p>\n\n<h4>Developer mode</h4>\n<p>When <strong>Developer mode</strong> is on (or no API key is set), nothing is sent — the message is written to the server console instead, so you can test signup without SMS credits:</p>\n<pre><code>[SMS:dev] to=8801712345678 :: Your Banglarfish verification code is 481920. It expires in 5 minutes.</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>A test that succeeds but shows nothing on the phone usually means Developer mode is still on.</li>\n<li>Register your <strong>Sender ID / masking</strong> with your provider first, or messages may be rejected.</li>\n<li>A gateway error (e.g. <code>responded 401</code>) points to a wrong API key, secret, or URL.</li>\n<li>Settings saved here override any <code>BOOMCAST_*</code> / <code>SMS_DEV_MODE</code> environment defaults.</li>\n<li>API Key and Secret Key are stored as password fields — treat them as sensitive.</li>\n</ul>"
  },
  {
    "slug": "social-login",
    "title": "Social login (Google/Facebook)",
    "category": "Configuration",
    "icon": "KeyRound",
    "summary": "Let customers sign in with Google or Facebook by pasting your own OAuth credentials into Admin → Social Login.",
    "html": "<p>Social login lets your customers sign in or sign up with one tap using <strong>Google</strong> or <strong>Facebook</strong>, instead of creating a password. This store handles OAuth itself (no third-party auth service), so it uses <em>your own</em> Google/Facebook app credentials tied to your domain. That means a short one-time setup: you create an OAuth app on each provider, paste the keys into <strong>Admin → Social Login</strong>, and the buttons go live.</p>\n\n<h3>Why don't the buttons appear yet?</h3>\n<p>The <em>Continue with Google / Facebook</em> buttons only show on the login &amp; signup page once a provider is <strong>Enabled</strong> here and saved with a valid Client ID. Until then, they stay hidden. There is nothing to \"turn on\" globally — each provider is independent.</p>\n\n<h3>Step by step</h3>\n<ol>\n<li>Go to <strong>Admin → Social Login</strong>.</li>\n<li>In the <strong>Google</strong> (or <strong>Facebook</strong>) card, click <strong>Copy</strong> next to the redirect URI. You'll paste this into the provider's console — copy it exactly.</li>\n<li><strong>Google:</strong> open <a href=\"https://console.cloud.google.com\">console.cloud.google.com</a> → APIs &amp; Services → OAuth consent screen (choose <em>External</em>, add your email) → Credentials → Create OAuth client ID → <strong>Web application</strong> → paste the copied URI under <em>Authorized redirect URIs</em>.</li>\n<li><strong>Facebook:</strong> open <a href=\"https://developers.facebook.com\">developers.facebook.com</a> → Create App → add the <strong>Facebook Login</strong> product → Settings → paste the copied URI under <em>Valid OAuth Redirect URIs</em>. Switch the app to <em>Live</em> mode.</li>\n<li>Copy the credentials back into this page: Google needs <strong>Client ID</strong> + <strong>Client Secret</strong>; Facebook needs <strong>App ID</strong> + <strong>App Secret</strong>.</li>\n<li>Tick <strong>Enabled</strong>, then click <strong>Save</strong>. The buttons appear on your auth page immediately.</li>\n</ol>\n\n<h3>The redirect URI</h3>\n<p>Each card shows the exact URI to register. It is built from your live domain, so it looks like:</p>\n<pre><code>https://yourstore.com/api/auth/google/callback\nhttps://yourstore.com/api/auth/facebook/callback</code></pre>\n<p>If the store runs under a subpath, that base is included automatically — always copy from the card rather than typing it by hand.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>The redirect URI must match <em>character for character</em> — a trailing slash or <code>http</code> vs <code>https</code> mismatch is the most common cause of \"redirect_uri_mismatch\" errors.</li>\n<li>Requested scopes are minimal: Google uses <code>openid email profile</code>, Facebook uses <code>email public_profile</code>. A verified email is required, so ask for the email permission.</li>\n<li>Keep the app in <strong>Live</strong> mode (Facebook) or publish the consent screen (Google) — test mode only works for accounts you list.</li>\n<li>Treat the Client/App Secret like a password. To rotate keys, paste the new secret and Save; untick <strong>Enabled</strong> to hide a provider without deleting its keys.</li>\n</ul>"
  },
  {
    "slug": "seo",
    "title": "SEO engine",
    "category": "Configuration",
    "icon": "Search",
    "summary": "Configure site-wide search settings, social sharing, structured data, and search-engine verification from Admin → SEO.",
    "html": "<p>The SEO engine controls how your store appears in Google, Bing, and social feeds. You set the defaults once under <strong>Admin → SEO</strong>, and every page inherits them unless you override the fields on an individual product, page, or post. Your <code>sitemap.xml</code> and <code>robots.txt</code> are generated for you automatically, so there are no files to upload.</p>\n\n<h3>How to use</h3>\n<ol>\n<li>Open <strong>Admin → SEO</strong>.</li>\n<li>Set the <strong>Title template</strong> (for example <code>%s | My Store</code>, where <code>%s</code> is filled in by each page's own title) and the <strong>Site name</strong>.</li>\n<li>Enter a <strong>Default title</strong>, <strong>Default description</strong>, and a <strong>Default OG image</strong>. These are used whenever a specific page has no SEO fields of its own.</li>\n<li>Add your <strong>Twitter/X handle</strong> and any <strong>social profile</strong> links (Facebook, Instagram, etc.).</li>\n<li>Fill in the <strong>Organization schema</strong> details. The system emits these as JSON-LD structured data so search engines understand your brand.</li>\n<li>Paste your <strong>Google</strong> and <strong>Bing verification</strong> codes to confirm ownership in their webmaster tools.</li>\n<li>Save. To fine-tune a single item, open its editor and use the <strong>per-product / page / post SEO fields</strong> to override the defaults.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Keep titles under ~60 characters and descriptions under ~155 so they aren't truncated in search results.</li>\n<li>Use a 1200×630px OG image so link previews look sharp on social platforms.</li>\n<li>The <strong>site-wide noindex</strong> switch tells search engines to ignore your whole store. Turn it on only for staging or a not-ready launch, and remember to turn it off before going live.</li>\n<li>Write unique per-product descriptions for your best sellers; the default is a safety net, not a substitute.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>The rendered <code>&lt;head&gt;</code> combines your defaults with any per-item overrides. A product page produces markup similar to this:</p>\n<pre><code>&lt;title&gt;Blue Widget | My Store&lt;/title&gt;\n&lt;meta name=\"description\" content=\"...\"&gt;\n&lt;meta property=\"og:image\" content=\"...\"&gt;\n&lt;meta name=\"twitter:site\" content=\"@mystore\"&gt;\n&lt;script type=\"application/ld+json\"&gt;\n{ \"@type\": \"Organization\", \"name\": \"My Store\" }\n&lt;/script&gt;</code></pre>\n<p>Verification codes surface as <code>&lt;meta name=\"google-site-verification\"&gt;</code> tags. The generated feeds live at <code>/sitemap.xml</code> and <code>/robots.txt</code> and refresh automatically as you add content.</p>"
  },
  {
    "slug": "users-roles",
    "title": "Users & Roles",
    "category": "Configuration",
    "icon": "Users",
    "summary": "Manage customers and staff, assign roles, block accounts, and ban abusive IP addresses from your store.",
    "html": "<p>The <strong>Users &amp; Roles</strong> page (in the admin sidebar) is where you manage everyone who touches your store: shoppers and the team who runs it. Each person has one <em>role</em> that decides what they can see and do. A companion page, <strong>Visitors &amp; IPs</strong>, lets you watch live traffic and block bad actors.</p>\n\n<h3>The role ladder</h3>\n<p>Roles are ranked from lowest to highest: <code>customer &lt; staff &lt; manager &lt; admin</code>. A higher role can do everything the roles below it can, plus more.</p>\n<ul>\n<li><strong>Customer</strong> — the default for anyone who signs up. They can shop and manage their own account, but have no admin access.</li>\n<li><strong>Staff</strong> — can manage the <strong>catalog</strong> (products, categories), <strong>orders</strong>, and <strong>content</strong> (pages, blog, media). They cannot change store configuration or user accounts.</li>\n<li><strong>Manager</strong> — everything staff can do, <em>plus</em> store <strong>configuration</strong> (payments, shipping, SMS, theme, settings) and managing user accounts.</li>\n<li><strong>Admin</strong> — full control of everything, including users and roles. The top of the ladder.</li>\n</ul>\n\n<h3>How to use it</h3>\n<ol>\n<li>Open <strong>Users &amp; Roles</strong> from the sidebar to see every account with its role, order count, total spent, and status.</li>\n<li>Click <strong>Add user</strong> to create a staff or customer account. Fill in name, email, phone, password, address, and pick a role.</li>\n<li>Click a user's <strong>edit</strong> button to change details or promote/demote their role using the <strong>Role</strong> dropdown.</li>\n<li>To suspend someone, use the <strong>Block</strong> button. Blocked users keep their data but cannot sign in. Click again to <strong>Unblock</strong>.</li>\n</ol>\n<p><em>Note:</em> adding, editing, blocking, and deleting accounts requires <strong>manager</strong> or <strong>admin</strong> access. You cannot block or delete your own account.</p>\n\n<h3>Visitors &amp; IPs (IP bans)</h3>\n<p>The <strong>Visitors &amp; IPs</strong> page has two tabs. <strong>Live visitors</strong> shows recent page views with IP, path, device, and location — click <strong>Ban</strong> on any row to block that IP instantly. <strong>Banned IPs</strong> lists current bans; add one manually with an IP and reason, or <strong>Unban</strong> to remove it. Bans are enforced across the whole site immediately.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>Give team members the <em>lowest</em> role that covers their job — start with staff and promote only when needed.</li>\n<li>Keep at least two admins so you are never locked out.</li>\n<li>Blocking is reversible; deleting is permanent. Prefer blocking for temporary problems.</li>\n<li>Use IP bans for scrapers or abusive traffic, not for individual customers — block the account instead.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Access is enforced server-side by rank guards, so hiding a menu item is not the security boundary — the handler check is:</p>\n<pre><code>const RANK = { customer: 0, staff: 1, manager: 2, admin: 3 };\nrequireStaff();    // read the users list\nrequireManager();  // create / update / block / delete users\nrequireAdmin();    // admin-only actions</code></pre>\n<p>Reading the user list needs <code>staff</code>; every mutation (create, update, status, delete) needs <code>manager</code> or above.</p>"
  },
  {
    "slug": "settings",
    "title": "Store settings",
    "category": "Configuration",
    "icon": "Settings",
    "summary": "Configure store-wide options: name, currency, delivery charges, tax, payment methods, social links, SEO defaults, and the announcement bar.",
    "html": "<p><strong>Admin → Settings</strong> is the control panel for your store's core, store-wide options. Everything on this page is one big form: change any field, then click <strong>Save changes</strong> at the top. Nothing is saved until you do — there is no autosave, and closing the page loses unsaved edits.</p>\n\n<h3>What you can configure</h3>\n<ul>\n<li><strong>Store Information</strong> — store name, email, phone, address, and currency (a text code like <code>BDT</code>).</li>\n<li><strong>Announcement Bar</strong> — the promo text for the strip at the top of the storefront.</li>\n<li><strong>Delivery</strong> — free delivery threshold (৳), standard delivery charge (৳), and tax/VAT (%).</li>\n<li><strong>Payment Gateways</strong> — toggle Cash on Delivery, bKash, Nagad, and SSLCommerz (cards) on or off.</li>\n<li><strong>Social Links</strong> — Facebook, Instagram, and YouTube profile URLs.</li>\n<li><strong>SEO Defaults</strong> — default meta title and meta description for search results and link previews.</li>\n</ul>\n\n<h3>Step by step</h3>\n<ol>\n<li>Open <strong>Admin → Settings</strong>.</li>\n<li>Edit the fields in each card. Delivery and tax fields accept numbers only.</li>\n<li>Flip the payment toggles for the methods you actually accept.</li>\n<li>Click <strong>Save changes</strong>. You'll see a \"Settings saved\" confirmation.</li>\n<li>Open your storefront in a new tab to confirm the change took effect.</li>\n</ol>\n\n<h3>How delivery and tax are applied</h3>\n<p>These values drive order totals automatically at checkout. If the cart total (after any discount) reaches the free delivery threshold, shipping is free; otherwise the standard charge applies. Tax is a straight percentage of the discounted subtotal:</p>\n<pre><code>shipping = subtotal >= freeShippingThreshold ? 0 : standardShipping\ntax      = round(subtotal * taxPercent / 100)\ntotal    = subtotal + shipping + tax</code></pre>\n<p>Set <strong>Tax / VAT</strong> to <code>0</code> if you don't charge tax.</p>\n\n<h3>Tips</h3>\n<ul>\n<li>Saving requires a <strong>Manager</strong> (or higher) role; other staff can view the page but not save. Each save is recorded in the audit log.</li>\n<li><strong>Announcement gotcha:</strong> the storefront's top bar reads its text from <strong>Admin → Branding</strong>, not from here. If the top bar doesn't update after saving Settings, edit the announcement under Branding too.</li>\n<li>Turn off a payment gateway you haven't configured yet, so customers aren't offered a method that can't complete.</li>\n<li>Keep the meta description under ~160 characters so search engines don't truncate it.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Settings are stored as a single JSON row in the <code>settings</code> table under the key <code>\"store\"</code> and merged over <code>defaultSettings</code> on read, so new fields fall back to defaults safely. The shape is:</p>\n<pre><code>type Settings = {\n  storeName; storeEmail; storePhone; currency; address;\n  freeShippingThreshold; standardShipping; taxPercent;\n  cod; bkash; nagad; sslcommerz;           // payment toggles\n  facebook; instagram; youtube;\n  metaTitle; metaDescription; announcement;\n}</code></pre>\n<p>Read/write via the <code>adminGetSettings</code> / <code>adminSaveSettings</code> server functions.</p>"
  },
  {
    "slug": "modules",
    "title": "Modules (feature toggles)",
    "category": "Configuration",
    "icon": "Blocks",
    "summary": "Turn built-in feature modules — Recipes, Blog, Masala band, Reviews, Wishlist, Newsletter and more — on or off from one screen, no code required.",
    "html": "<p>Modules are your store's built-in <strong>\"plugin\" layer</strong>. Each module is a self-contained feature — <em>Recipes, Blog, the Masala band, Reviews, Wishlist, Newsletter, Visitor tracking,</em> and more — that you can switch on or off from a single screen. This is the safe, no-code way to shape your store: no files to edit, no deploys, and no risk of breaking checkout.</p>\n\n<h3>What happens when you toggle a module off</h3>\n<p>Turning a module off instantly hides its <strong>storefront surfaces</strong> — the parts customers see. Switch off the Masala band and it vanishes from the homepage; switch off Reviews and the review block disappears from product pages; switch off Wishlist and the heart icons go away. Your data is never deleted — flip the switch back on and everything returns exactly as it was.</p>\n\n<h3>How to use</h3>\n<ol>\n  <li>Open <strong>Admin → Modules</strong>.</li>\n  <li>Find the module in the list. Each row shows its name and a short description of what it controls.</li>\n  <li>Click the toggle to turn it <strong>on</strong> (enabled) or <strong>off</strong> (disabled).</li>\n  <li>The change saves immediately. Open your storefront in another tab and refresh to confirm the surface appeared or disappeared.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n  <li>A toggle only controls <em>visibility</em>. Your recipes, posts, and reviews stay safe in the admin even while a module is off.</li>\n  <li>Some modules pair with a content screen — e.g. Recipes with <strong>Admin → Recipes</strong>, Blog with <strong>Admin → Blog</strong>. Enable the module, then add content there.</li>\n  <li>Homepage sections (Masala band, Recipes, Blog, Newsletter) also need content: an enabled section may stay hidden until you add items.</li>\n  <li>Turning off <strong>Visitor tracking</strong> stops recording page views, so <strong>Admin → Analytics</strong> and <strong>Visitors &amp; IPs</strong> go quiet.</li>\n  <li>Disable modules you don't use to keep the store lean and the homepage focused.</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Each storefront surface is guarded behind its module flag, so a disabled module never renders and never runs its queries. Flags are plain booleans persisted in site config:</p>\n<pre><code>{settings.masala &amp;&amp; &lt;MasalaSection ... /&gt;}\n{settings.reviews &amp;&amp; &lt;ProductReviews ... /&gt;}\n{settings.newsletter &amp;&amp; &lt;NewsletterSignup /&gt;}</code></pre>\n<p>To add a new module, define a boolean flag and gate its component the same way — no other wiring required.</p>"
  },
  {
    "slug": "dev-architecture",
    "title": "Architecture (for developers)",
    "category": "Developer",
    "icon": "Layers",
    "summary": "How this store is built: TanStack Start SSR, PostgreSQL + Drizzle, self-hosted auth, and server functions that keep server code off the client.",
    "html": "<p>This store is a single full-stack app built on <strong>TanStack Start</strong> (React 19 + Vite with server-side rendering). At build time Vite uses <strong>nitro</strong>'s <code>node-server</code> preset to produce one standalone Node process. Data lives in <strong>PostgreSQL</strong>, accessed through the <strong>Drizzle ORM</strong>. Auth is self-hosted (no third-party service), and business logic runs in <em>server functions</em> so your database and secrets never reach the browser.</p>\n\n<h3>Folder layout</h3>\n<ul>\n<li><code>src/routes</code> — file-based pages. The <code>admin.*</code> files (e.g. <code>admin.products.tsx</code>) are the admin screens; <code>$slug</code> files are dynamic public pages.</li>\n<li><code>src/lib/*.functions.ts</code> — server functions built with <code>createServerFn</code>. This is your API layer.</li>\n<li><code>src/server</code> — server-only code: <code>db</code> (Drizzle schema + client), <code>auth</code> (password, session, oauth, otp), <code>settings</code>/<code>site-config</code>.</li>\n<li><code>src/components</code> — shared React UI.</li>\n</ul>\n\n<h3>How server functions work</h3>\n<p>A server function is callable from React like a normal async function, but its body only ever runs on the server. Crucially, it <strong>dynamically imports</strong> from <code>@/server/*</code> inside the handler, so that server code is never bundled into the client:</p>\n<pre><code>export const listCategories = createServerFn({ method: \"GET\" })\n  .handler(async () => {\n    const { db } = await import(\"@/server/db\");\n    const { categories } = await import(\"@/server/db/schema\");\n    return db.select().from(categories);\n  });</code></pre>\n\n<h3>Auth</h3>\n<p>Passwords are hashed with Node's built-in <code>scrypt</code> (no native build step). Sessions are <em>opaque</em>: the <code>bf_session</code> cookie holds a random token, and only its SHA-256 hash is stored in the <code>sessions</code> table.</p>\n\n<h3>Config storage</h3>\n<p>Settings are not files. They live in the <code>settings</code> table as <strong>jsonb per key</strong> (for example <code>store</code>, <code>homepage</code>, <code>theme</code>), read and written through helpers in <code>src/server/settings.ts</code> using upserts.</p>\n\n<h3>How to add a feature (step by step)</h3>\n<ol>\n<li>If you need new data, add a table in <code>src/server/db/schema.ts</code>, then run <code>npm run db:generate</code> and <code>npm run db:migrate</code>.</li>\n<li>Add a server function in a <code>src/lib/*.functions.ts</code> file; dynamically import your <code>@/server/*</code> deps inside the handler.</li>\n<li>Validate inputs with a <code>zod</code> schema via <code>.validator()</code>.</li>\n<li>Add or edit a route under <code>src/routes</code> and call the function from the component.</li>\n</ol>\n\n<h3>Tips</h3>\n<ul>\n<li>Never import <code>@/server/*</code> at the top level of a client file — keep those imports inside server-function handlers.</li>\n<li>Deploy runs via <strong>systemd + nginx</strong>; a sub-path deploy (e.g. <code>/client1</code>) is set with <code>VITE_BASE_PATH</code> at build time. Use <code>withBase()</code> for raw <code>window.location</code> navigations.</li>\n<li>Store new configuration as another key in the <code>settings</code> table rather than adding env vars.</li>\n</ul>"
  },
  {
    "slug": "dev-theming",
    "title": "Building themes & presets (for developers)",
    "category": "Developer",
    "icon": "Palette",
    "summary": "How the Theme object becomes live CSS, plus how to add presets and new surfaces, backgrounds, heroes, or fonts.",
    "html": "<p>The whole storefront is styled from a single <strong>Theme</strong> object — colors, <code>radius</code>, <code>surface</code>, <code>hero</code>, <code>bg</code>, fonts, the <code>blur/opacity/depth/glow</code> sliders, boolean effects, and raw <code>customCss</code>. If you just want to recolor the shop, you don't need any of this: open the Theme editor in the admin, tweak the colors and paste snippets into <strong>Custom CSS</strong>. This page is for adding new <em>presets</em> and new <em>surface / background / hero</em> options in code.</p>\n\n<h3>How it works</h3>\n<p><code>themeToCss()</code> in <code>src/lib/theme-presets.ts</code> turns a Theme into <code>:root</code> CSS variables (<code>--primary</code>, <code>--radius</code>, <code>--bf-blur</code>, <code>--bf-depth</code>, …) plus the <code>#bf-root</code> background. The app also stamps <code>data-*</code> attributes onto <code>#bf-root</code> (<code>data-surface</code>, <code>data-bg</code>, <code>data-animations</code>, …). The actual look of surfaces and effects lives in <code>src/styles.css</code>, keyed off those <code>data-*</code> attributes and the <code>--bf-*</code> vars. Colors are validated by <code>safeColor()</code> (hex / rgb / hsl / oklch) and numbers are clamped, so out-of-range values snap back to defaults.</p>\n\n<h3>Step by step: add a preset</h3>\n<ol>\n<li>Add an entry to <code>THEME_PRESETS</code> built with <code>mk({...})</code> so every field is filled from <code>FULL_DEFAULT</code>.</li>\n<li>Add a matching row to <code>PRESET_META</code> so it shows in the gallery.</li>\n</ol>\n<pre><code>// src/lib/theme-presets.ts\nTHEME_PRESETS.forest = mk({\n  preset: \"forest\",\n  primary: \"#166534\", brand: \"#ca8a04\", accent2: \"#22c55e\",\n  background: \"#f0fdf4\", card: \"#ffffff\", foreground: \"#14251b\",\n  surface: \"neo\", hero: \"split\", bg: \"mesh\",\n});\n// PRESET_META.push:\n{ key: \"forest\", label: \"Forest\", desc: \"Deep green, earthy\", category: \"nature\" }</code></pre>\n\n<h3>Add a new surface / background / hero</h3>\n<ol>\n<li>Extend the union in <code>src/lib/types.ts</code> (<code>ThemeSurface</code>, <code>ThemeBg</code>, or <code>ThemeHero</code>).</li>\n<li>Add the same value to the matching <code>z.enum([...])</code> in <code>theme.functions.ts</code> — or saves will fail validation.</li>\n<li>Add the CSS in <code>styles.css</code>, keyed off the attribute.</li>\n<li>For a hero, also add the layout to the Hero component in <code>src/routes/index.tsx</code>.</li>\n</ol>\n<pre><code>#bf-root[data-surface=\"frost\"] .bg-card {\n  background-color: color-mix(in srgb, var(--card) 70%, transparent);\n  backdrop-filter: blur(var(--bf-blur, 16px));\n}</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Always build presets with <code>mk()</code> — partial themes break the type and the merge.</li>\n<li>Keep the union type, the zod enum, and the CSS in sync; a mismatch means a rejected save.</li>\n<li>New fonts must be added to <code>FONT_STACK</code> <em>and</em> the Google Fonts <code>&lt;link&gt;</code> in <code>__root.tsx</code>.</li>\n<li><code>customCss</code> is appended after the generated theme — scope selectors under <code>#bf-root</code> so it never leaks into the admin.</li>\n</ul>"
  },
  {
    "slug": "dev-extending",
    "title": "Extending the CMS / modules (for developers)",
    "category": "Developer",
    "icon": "Blocks",
    "summary": "Safely add admin pages, data tables, server functions, config keys, and feature toggles by editing the codebase directly.",
    "html": "<p>This CMS has no uploadable-plugin system. You extend it by editing the codebase directly, following a few consistent patterns. This keeps every feature type-checked, guarded, and easy to audit. Below is how to add each kind of piece safely.</p>\n\n<h3>Step by step: add a new feature</h3>\n<ol>\n<li><strong>New admin page.</strong> Create <code>src/routes/admin.&lt;name&gt;.tsx</code> (file-based routing turns this into <code>/admin/&lt;name&gt;</code>), then add a nav entry to the <code>navGroups</code> array in <code>src/routes/admin.tsx</code> so it appears in the sidebar.</li>\n<li><strong>New data.</strong> Add a Drizzle table to <code>src/server/db/schema.ts</code>, then run <code>db:push</code> to create it in Postgres. Use <code>jsonb</code> columns for flexible/nested data (see existing tables like <code>orders</code> or <code>products</code>).</li>\n<li><strong>Server functions.</strong> Put logic in <code>src/lib/&lt;name&gt;.functions.ts</code> using <code>createServerFn</code>. Validate every input with <code>zod</code> and guard admin actions with <code>requireStaff</code> or <code>requireManager</code>. Call them from your page with <code>useServerFn</code>.</li>\n<li><strong>New config.</strong> Store settings as a jsonb key in the <code>settings</code> table via the site-config pattern (a <code>getXConfig</code>/<code>saveXConfig</code> pair with defaults), like payments and SMS do.</li>\n<li><strong>Feature on/off.</strong> Wire a <strong>Module toggle</strong> (a boolean in a config key) so staff can enable or disable the feature without a redeploy.</li>\n</ol>\n\n<h4>Server function example</h4>\n<pre><code>export const adminUpsertThing = createServerFn({ method: \"POST\" })\n  .inputValidator((i: unknown) =&gt; ThingInput.parse(i))\n  .handler(async ({ data }) =&gt; {\n    const { requireManager } = await import(\"@/server/auth/context\");\n    const { db } = await import(\"@/server/db\");\n    const { things } = await import(\"@/server/db/schema\");\n    await requireManager();\n    await db.insert(things).values(data);\n    return { ok: true };\n  });</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Keep secrets and DB access <strong>only inside <code>@/server</code></strong>, loaded with dynamic <code>import()</code> inside handlers — never import them into client/route files, or credentials leak to the browser.</li>\n<li><strong>Always</strong> validate input with zod and <strong>always</strong> guard admin functions; <code>requireStaff</code> for general admin work, <code>requireManager</code> for sensitive changes.</li>\n<li>Reuse the <code>onConflictDoUpdate</code> upsert style from <code>src/server/site-config.ts</code> for config saves.</li>\n<li>Log important changes via the existing <code>audit</code> helper so actions are traceable.</li>\n<li>This edit-the-code approach is the safe, reviewable alternative to arbitrary uploadable plugins.</li>\n</ul>"
  },
  {
    "slug": "dev-security",
    "title": "Security model",
    "category": "Developer",
    "icon": "ShieldCheck",
    "summary": "How this CMS protects accounts, sessions, and data: hashed passwords, guarded server functions, server-side recomputation, and why no arbitrary code upload.",
    "html": "<p>This page explains how the store keeps accounts, sessions, and money safe. You don't have to configure any of it — the protections are always on — but knowing how they work helps you extend the system without accidentally opening a hole.</p>\n\n<h3>What protects your store</h3>\n<ul>\n<li><strong>Password hashing:</strong> passwords are stored using <code>scrypt</code>, never in plain text. Even with database access, they can't be read back.</li>\n<li><strong>Opaque session cookies:</strong> logging in sets an <code>HttpOnly</code> cookie (so JavaScript can't read it) with <code>SameSite</code> protection, and <code>Secure</code> in production so it only travels over HTTPS. The cookie is a random token, not your data.</li>\n<li><strong>Role-based guards:</strong> every admin and server function starts with a check — <code>requireUser</code>, <code>requireStaff</code>, or <code>requireManager</code>. Roles rank <em>customer &lt; staff &lt; manager &lt; admin</em>, so a manager guard also admits admins.</li>\n<li><strong>Input validation:</strong> all inputs pass through <code>zod</code> schemas before anything runs.</li>\n<li><strong>Server-only isolation:</strong> sensitive code lives under <code>@/server</code> and is loaded via dynamic import, so it is never bundled into the browser.</li>\n<li><strong>Trust nothing from the client:</strong> prices, stock, coupons, and delivery fees are always recomputed on the server. Values sent by the browser are ignored.</li>\n<li><strong>IP bans</strong> are enforced on the server for blocked visitors.</li>\n</ul>\n\n<h3>How to use it (developers)</h3>\n<ol>\n<li>Create your server logic inside <code>@/server</code> so it never reaches the client bundle.</li>\n<li>Guard the entry point first, then validate, then run:</li>\n</ol>\n<pre><code>const user = await requireManager();\nconst input = MySchema.parse(rawInput); // zod\n// ...trusted work here (recompute totals server-side)</code></pre>\n<ol start=\"3\">\n<li>Never trust client-sent totals — recompute prices, stock, and discounts yourself.</li>\n</ol>\n\n<h3>Custom Code / CSS</h3>\n<p>The Custom Code and CSS panels are <strong>manager-only</strong> and strictly <strong>front-end</strong> — they inject markup and styles into the storefront. They <em>cannot</em> run server code.</p>\n\n<h3>Why no plugin uploads or server-code editor?</h3>\n<p>Letting the admin upload plugins or edit server files would be <strong>remote code execution</strong>: a single compromised account could take over the whole box. We deliberately don't offer it. Instead, extend the platform in the codebase and ship it with git:</p>\n<pre><code>git add . &amp;&amp; git commit -m \"feature\"\ngit push   # deploy pipeline builds &amp; restarts</code></pre>\n\n<h3>Tips</h3>\n<ul>\n<li>Add the tightest guard that still works — prefer <code>requireManager</code> over <code>requireUser</code> for admin actions.</li>\n<li>Give staff the lowest role that covers their job.</li>\n<li>Keep secrets in environment variables, never in Custom Code.</li>\n<li>Test guards by calling a server function while signed out — it should be rejected.</li>\n</ul>"
  },
  {
    "slug": "email-system",
    "title": "Email system, mail server & DKIM",
    "category": "Configuration",
    "icon": "Mail",
    "summary": "How the self-hosted mail server sends and receives, the SPF/DKIM/DMARC and MX DNS records, the in-panel email client (Inbox/Sent/Compose), and the support-only login.",
    "html": "<p>Your store runs its <strong>own mail server</strong> (Postfix + OpenDKIM) on the VPS. It sends transactional email from <code>no-reply@banglarfish.com</code> with no third-party provider (no Resend, no Brevo). This page explains how it works, the DNS records it needs, and the full email client built into the admin panel.</p>\n\n<h3>Sending (outbound)</h3>\n<p>Every account and order event sends automatically: <strong>welcome</strong> (signup), <strong>password reset</strong>, <strong>order confirmation</strong>, and <strong>order status</strong>. Wording is editable under <em>Configuration &rarr; Email</em> (email templates) and <em>Configuration &rarr; SMS Gateway</em> (SMS templates). The app connects to the local mail server over the loopback interface, and OpenDKIM signs each message.</p>\n\n<h3>DNS records (required for delivery)</h3>\n<p>These live in your DNS (Cloudflare). <strong>SPF, DKIM and DMARC</strong> make inboxes trust your mail; without them Gmail rejects everything.</p>\n<ul>\n<li><strong>SPF</strong> &mdash; TXT on the root (<code>@</code>): <code>v=spf1 ip4:YOUR.SERVER.IP ~all</code>. <strong>Keep exactly ONE SPF record</strong> &mdash; two <code>v=spf1</code> records is a hard error that blocks all mail.</li>\n<li><strong>DKIM</strong> &mdash; TXT at <code>mail._domainkey</code>: the public key printed by the server (<code>/etc/opendkim/keys/banglarfish.com/mail.txt</code>).</li>\n<li><strong>DMARC</strong> &mdash; TXT at <code>_dmarc</code>: <code>v=DMARC1; p=none; rua=mailto:you@example.com</code>.</li>\n</ul>\n\n<h3>Receiving (inbound) &mdash; the Inbox</h3>\n<p>Mail sent <em>to</em> any <code>@banglarfish.com</code> address (support@, info@, sales@&hellip;) is delivered into the admin Inbox. To turn this on, add two DNS records (DNS-only / grey-cloud &mdash; Cloudflare cannot proxy mail):</p>\n<ul>\n<li><strong>MX</strong> on the root: mail server <code>mail.banglarfish.com</code>, priority <code>10</code>.</li>\n<li><strong>A</strong> record <code>mail</code> &rarr; <code>YOUR.SERVER.IP</code> (Proxy status: <strong>DNS only</strong>).</li>\n</ul>\n<p>Because you receive on your own server, do <strong>not</strong> also enable Cloudflare Email Routing &mdash; the two would set conflicting MX records.</p>\n\n<h3>The email client (Configuration &rarr; Email Inbox / Send)</h3>\n<ul>\n<li><strong>Inbox</strong> &mdash; incoming mail (once the MX record is live).</li>\n<li><strong>Sent</strong> &mdash; a copy of every email the system sends, plus anything you compose.</li>\n<li><strong>Compose</strong> &mdash; email any address from your store address, with <strong>Cc/Bcc</strong>, a visual/HTML editor, and <strong>attachments</strong> (images + PDF).</li>\n<li><strong>Reply / Forward</strong> from any message.</li>\n</ul>\n\n<h3>Support-only login</h3>\n<p>To let a support person handle email without touching the rest of the admin: open <em>Configuration &rarr; Users &amp; Roles</em>, add a user (or edit one) and set the role to <strong>Support</strong>. When they sign in they see <strong>only</strong> the email client &mdash; every other admin page is blocked.</p>\n\n<h3>Troubleshooting</h3>\n<ul>\n<li><strong>Bounced with \"SPF did not pass\"</strong> &mdash; almost always a duplicate SPF record. Keep exactly one.</li>\n<li><strong>Lands in spam</strong> &mdash; normal for a brand-new server IP; it improves as you send real mail. Mark one message \"Not spam\".</li>\n<li><strong>Inbox stays empty</strong> &mdash; the MX + <code>mail</code> A record aren't added yet, or the A record is proxied (must be DNS-only).</li>\n</ul>\n\n<h3>For developers</h3>\n<p>Outbound: <code>src/server/email.ts</code> (provider-agnostic SMTP/Resend; loopback SMTP skips TLS verification for the self-signed local cert). Every send is logged to the <code>email_messages</code> table via <code>src/server/mailbox.ts</code>. Admin fns live in <code>src/lib/mail.functions.ts</code>, guarded by <code>requireMailAccess()</code> (support + staff + manager + admin). Inbound: Postfix routes <code>banglarfish.com</code> via a <code>transport_maps</code> pipe to <code>/usr/local/bin/store-mail.py</code>, which inserts rows with an INSERT-only DB role.</p>\n<pre><code>// send + auto-log to Sent\nawait sendEmail({ to, subject, html, cc, bcc, attachments, category: \"manual\" });</code></pre>"
  }
];
